#pragma once

#include "common/Data.h"

namespace eve::thread
{

/**
 * @brief Task is a kind of job than can be executed by a thread.
 * and you append additional information to the task. 
 * Once it finished, you can append other task or get the result.
 * Task could be visualized in the devtool.
 */
class Task {
public:



protected:
    // threadPool
};

} // namespace thread
