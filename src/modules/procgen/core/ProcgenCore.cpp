#include "procgen/core/ProcgenCore.h"
// This translation unit intentionally has no implementation. It makes the
// curated core umbrella part of the core-only compile target, so a new
// presentation include cannot hide behind an uncompiled header.
