# Geometry Stroke Lab

Builds and renders the three geometry-painting cross-sections from an owning runtime input trail:
`quad`, `triangularPrism`, and `cube`. The example covers planar and spatial input, minimum-spacing
filtering, undo, CPU mesh generation, and GPU upload.
