# Waterfall — 瀑布流动着色器

垂直瀑布面片：向下流动条纹、湍流、顶部水唇泡沫与底部溅落池，
含颜色 / 反射强度 / 太阳强度等可调参数。

## 运行

```bash
make run/<platform>-debug GAME=examples/waterfall-demo
```

启动约 1.5 秒后自动把当前帧保存到 `/tmp/waterfall_demo.png`，也可按 `F` 随时保存。
