# VRM Avatar

将 VRoid Studio 导出的 VRM 1.0 文件复制为本目录的 `model.vrm`，从仓库根目录运行：

```
make run/win32-debug GAME=examples/vrm-avatar
```

自动展示眨眼、视线跟踪、头部转动与头发弹簧骨骼。空格切换六种表情，P 暂停/恢复自动头部动作。
模型文件不随示例分发。运行时导入失败会显示结构化诊断。
