# Inventory 示例

演示 `eve.Inventory()` 背包模块：JSON 物品定义、主背包 / 任务栏 / 仓库、装备穿脱与跨包转移。

```bash
make run/<platform>-debug GAME=examples/inventory
# 或
make inventory
```

按键：`1` 拾取 · `2` 喝药 · `3` 装备/卸下 · `4` 存矿 · `R` 重置
