# 独立资源格式样本

这些样本由本仓库 `scripts/generate_resource_format_fixtures.py` 从原创像素、正弦波 PCM 和单个三角形生成，按 CC0-1.0 提供；不包含第三方美术或音频素材。生成工具版本、命令、期望内容及文件 SHA-256 见 `manifest.json`。

正常测试使用已提交样本。需要重新生成时安装 Pillow 和 FFmpeg 后运行脚本，并审查内容和哈希变化；编码器版本变化可能产生不同字节。生成过程不调用引擎编码器，以免导入和导出缺陷互相掩盖。

样本只证明相应最小内容契约，不代表所有格式特性均受支持。
