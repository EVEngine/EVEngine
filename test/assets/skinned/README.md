# Skinned character fixtures

Run `scripts/download_skinned_character.sh` (or `make download-skinned-character`) to fetch **CesiumMan** into `cesium_man/glTF/`.

See [ATTRIBUTION.md](ATTRIBUTION.md) for license and CMake options.
