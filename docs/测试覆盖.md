# EVEngine 模块 API 单元测试覆盖

> 状态：已讨论确认（方案：人工维护 Markdown 覆盖矩阵 + 约定驱动补测）。  
> 范围约定：以 **C++ 公开头文件 API** 为准；达标标准 = **happy-path + 主要错误/边界路径**。  
> 本轮样板模块：`data`、`filesystem`。其余模块先列入总表，API 明细后续展开。  
> 相关模块清单见 [模块设计.md](./模块设计.md)。测试框架见 [zeroerr 设计](./superpowers/specs/2026-08-04-zeroerr-test-framework-design.md)。  
> 实现计划：[2026-08-05-api-unit-test-coverage.md](./superpowers/plans/2026-08-05-api-unit-test-coverage.md)。

## 1. 目标与非目标

### 1.1 目标

- 建立跨模块 C++ API 单测规范（zeroerr + 现有 `test/`）
- 维护本文档作为长期进度表：模块总表 + API 明细（可分多轮推进）
- 本轮落地样板：`data`、`filesystem`（清单展开 + 缺口补测至达标标准）

### 1.2 非目标

- 不测脚本绑定层（Squirrel / `.nut` 冒烟不计入本矩阵）
- 不引入覆盖率工具或 YAML/JSON 自动化清单
- 不为本轮补齐未实现模块的实现与测试
- 不改模块公共 API 语义（仅补测；发现 bug 另开修复）

## 2. 覆盖矩阵约定

### 2.1 模块总表列

| 列 | 含义 |
|---|---|
| 模块 | 如 `data`、`filesystem` |
| 头文件入口 | 主公开头文件路径 |
| 实现状态 | 与模块设计对齐：`可用` / `部分` / `未实现` |
| API 展开 | `已展开` / `待展开` / `N/A` |
| 覆盖进度 | `x/y 已覆盖`；未展开为 `-` |
| 备注 | 依赖设备、阻塞项等 |

### 2.2 API 明细表列

| 列 | 含义 |
|---|---|
| API | `Class::method` 或自由函数（重载写清签名要点） |
| 头文件 | 声明所在文件 |
| 状态 | `未测` / `部分` / `已覆盖` / `N/A` |
| 用例 | `TEST_CASE` 全名（可多条，逗号分隔） |
| 缺口 | 缺的 happy / 错误 / 边界说明 |

### 2.3 状态判定

| 状态 | 含义 |
|---|---|
| **已覆盖** | 至少 1 条 happy-path，且覆盖该 API 主要错误/边界（非法参数、空输入、失败返回等，按语义） |
| **部分** | 有用例触及，但未达上述标准 |
| **未测** | 无对应用例 |
| **N/A** | 未实现、无可独立测点、或仅平台桩（本机跳过并在备注说明） |

### 2.4 清单来源

- 以公开头文件中的 public 方法 / 自由函数为准
- 不含 `private`、匿名命名空间、`Module_REG` 宏细节
- 构造/析构/赋值：仅当行为有契约（所有权、空对象、clone）时单独成行；平凡默认析构可不列
- 后端特化类（如 `physfs::Filesystem`）不单独列行；测抽象接口（如 `Filesystem`）即可
- 全模块进总表；未实现模块 API 展开为 `N/A`，不展开方法级明细，直至模块可用

## 3. 测试约定

### 3.1 框架与布局

- 使用 zeroerr：`TEST_CASE` / `CHECK` / `REQUIRE`
- C++ API 测放在 `test/<module>.cpp`（可按子域拆文件，并登记进 `test/CMakeLists.txt`）
- 现有 `.nut` 脚本冒烟可保留，**不计入**本覆盖矩阵
- zeroerr 过滤请用整名匹配，例如：`--testcase=^data\..*$`（仅 `^data\.` 可能匹配 0 条）

### 3.2 命名

格式：`Module.Subject.behavior`

示例：

- `data.ByteData.roundTrip`
- `data.JsonDocument.decodeInvalid`
- `filesystem.mount.missingArchiveReturnsFalse`

### 3.3 Fixture

- **data**：直接构造 `DataModule` / 相关类型，避免不必要的全局单例
- **filesystem**：`Filesystem::create()` 为进程单例且构造已 `init`——**勿再调 `init`**；每用例独立 `setIdentity` + `setupWriteDirectory`；写路径测后清理

### 3.4 本轮交付

1. 本文档：规范 + 全模块总表 + `data` / `filesystem` API 展开与现状标注
2. 按表补齐 `data`、`filesystem` 缺口至「已覆盖」（允许个别 `部分`/`N/A` 并备注）
3. 本地跑通相关 `unit_test` / CTest

### 3.5 后续轮次

按总表优先级展开下一模块 API → 补测 → 更新本文档进度。不要求一轮完成全部模块。

## 4. 错误处理与维护流程

- **断言失败**：按 API 契约区分返回空指针 / 抛异常 / 返回 false；用例应对准真实契约，不为迁就测试改 API
- **发现实现 bug**：先记入该 API「缺口」或备注；修复与补测可同 PR，也可分 PR
- **新增公开 API**：合并前在对应明细表增加一行，状态 `未测`，并尽量同批补测
- **废弃 / 未实现**：标 `N/A` 并写明原因，勿静默删行（保留历史可见性）
- **进度更新**：补测合并时同步更新「状态 / 用例 / 覆盖进度」列

## 5. 模块总表

| 模块 | 头文件入口 | 实现状态 | API 展开 | 覆盖进度 | 备注 |
|------|------------|----------|----------|----------|------|
| filesystem | `src/modules/filesystem/Filesystem.h` | 可用 | 已展开 | **28/40 已覆盖**（4 部分 / 若干未测·N/A） | C++：`test/filesystem_cpp.cpp`；`.nut` 不计入 |
| event | `src/modules/event/Event.h` | 部分 | 已展开 | **见 §9** | `test/event.cpp`；`Event::wait` 未测 |
| mouse | `src/modules/mouse/Mouse.h` | 可用 | 已展开 | **见 §10** | `test/mouse_cpp.cpp` |
| keyboard | `src/modules/keyboard/Keyboard.h` | 部分 | N/A | **N/A** | 未 `create_module` / 无 `Module_REG`；见 kb-joy 报告 |
| touch | `src/modules/touch/Touch.h` | 可用 | 已展开 | **2/2 已覆盖** | `test/touch.cpp`；`EVTouch` 已链入 unit_test |
| joystick | `src/modules/joystick/Joystick.h` | 部分 | N/A | **N/A** | 未接入构建 + `love::` 残留 |
| window | `src/modules/window/Window.h` | 可用 | 已展开 | **见 §11** | `test/window_cpp.cpp`（14 用例） |
| graphics | `src/modules/graphics/Graphics.h` | 部分 | 待展开 | - | 含 GPU/窗口依赖 |
| data | `src/modules/data/DataModule.h` | 可用 | 已展开 | **33/35 已覆盖**（2 部分） | `test/data.cpp`；22 个 `data.*` 用例 |
| image | `src/modules/image/Image.h` | 可用 | 已展开 | **见 §12** | `test/image.cpp`（10 用例） |
| sound | `src/modules/sound/Sound.h` | 可用 | 已展开 | **见 §13** | `test/sound.cpp`（7 用例） |
| model3d / 3dmodel | `src/modules/model3d/Model3D.h` | 可用 | 已展开 | **见 §13b** | `test/model3d.cpp`（5 用例）；medialoader Assimp |
| audio | `src/modules/audio/Audio.h` | 可用 | 已展开 | **见 §14** | `test/audio.cpp`（9 用例）；需 OpenAL |
| plugins | — | 未实现 | N/A | - | |
| system | `src/modules/system/System.h` | 可用 | 已展开 | OS/CPU/RAM/墙钟/休眠/剪贴板/电量/GPU | `test/system.cpp`（6 用例） |
| network | `src/modules/network/Network.h` | 可用 | 已展开 | **见 §15** | `test/network.cpp`（9 network.*） |
| thread | `src/modules/thread/Thread.h` | 部分 | 待展开 | - | |
| compute | — | 未实现 | N/A | - | |
| particles | `src/modules/particles/` | 可用 | 已展开 | Camera/Canvas + 发射区域/力场 + JSON 热更 | `test/particles.cpp`（18 用例） |
| box2d | `src/modules/physics/` | 可用 | 已展开 | World/Body/Fixture + meter + event 碰撞 | `test/box2d.cpp`（8 用例） |
| animation | — | 未实现 | N/A | - | |
| map | — | 未实现 | N/A | - | |
| database | — | 未实现 | N/A | - | |
| math | — | 未实现 | N/A | - | |
| entity | `src/modules/entity/EntityModule.h` / ECS | 可用 | 待展开 | - | `test/ECS.cpp` 已有基础用例 |
| timer | `src/modules/timer/Timer.h` | 可用 | 已展开 | **3/3 已覆盖** | `test/timer_cpp.cpp`；`.nut` 不计入 |

## 6. API 明细：`data`

现状摘要（2026-08-05 样板轮）：`test/data.cpp` 共 22 个 `TEST_CASE`，回归 `--testcase=^data\..*$` 全部 PASS。

### 6.1 自由函数（`DataModule.h`）

| API | 头文件 | 状态 | 用例 | 缺口 |
|-----|--------|------|------|------|
| `compress(format, raw, size, level)` | DataModule.h | 已覆盖 | `data.compress.lz4RoundTrip`, `data.compress.invalidFormatThrows` | |
| `decompress(CompressedData*, size&)` | DataModule.h | 已覆盖 | `data.compress.lz4RoundTrip` | |
| `decompress(format, cbytes, size, rawsize&)` | DataModule.h | 已覆盖 | `data.decompress.invalidFormatThrows`（非法 format）；happy 经 CompressedData 重载 | |
| `encode(format, src, srclen, dstlen, linelen)` | DataModule.h | 部分 | `data.encode.hexAndBase64RoundTrip` | 非 hex 一律走 base64，无「非法 format」抛错契约 |
| `decode(format, src, srclen, dstlen)` | DataModule.h | 部分 | `data.encode.hexAndBase64RoundTrip` | 同上；缺损坏输入边界 |
| `hash(function, Data*)` → string | DataModule.h | 已覆盖 | `data.hash.md5KnownAndInvalid` | |
| `hash(function, char*, size)` → string | DataModule.h | 已覆盖 | `data.hash.md5KnownAndInvalid` | |
| `hash(function, Data*, Value&)` | DataModule.h | 已覆盖 | `data.hash.md5KnownAndInvalid`（经 string 路径 + Value 重载） | |
| `hash(function, char*, size, Value&)` | DataModule.h | 已覆盖 | `data.hash.md5KnownAndInvalid` | |

### 6.2 `DataModule`

| API | 头文件 | 状态 | 用例 | 缺口 |
|-----|--------|------|------|------|
| `newDataView(data, offset, size)` | DataModule.h | 已覆盖 | `data.DataView.windowAndBounds` | |
| `newByteData(size)` | DataModule.h | 已覆盖 | `data.DataModule.newByteDataFactories`；size=0 见 `data.ByteData.sizeZeroThrows` | |
| `newByteData(const void*, size)` | DataModule.h | 已覆盖 | `data.DataModule.newByteDataFactories` | |
| `newByteData(void*, size, own)` | DataModule.h | 已覆盖 | `data.DataModule.newByteDataFactories`, `data.ByteData.ownFalseCopies` | |
| `newJsonDocument()` | DataModule.h | 已覆盖 | `data.JsonEmptyNew` | |
| `decodeJson(text)` | DataModule.h | 已覆盖 | `data.Json.decodeWithoutErrorPtr` | |
| `decodeJson(text, error*)` | DataModule.h | 已覆盖 | `data.JsonObjectRoundTrip`, `data.JsonArrayRoot`, `data.JsonInvalid` | |
| `decodeJson(Data*, error*)` | DataModule.h | 已覆盖 | `data.JsonFromByteData` | |
| `encodeJson(doc, pretty)` | DataModule.h | 已覆盖 | `data.JsonObjectRoundTrip`, `data.JsonEmptyNew` | |
| `encodeJsonData(doc, pretty)` | DataModule.h | 已覆盖 | `data.JsonFromByteData` | |
| `newXmlDocument()` | DataModule.h | 已覆盖 | `data.XmlEmptyNew` | |
| `decodeXml(text)` | DataModule.h | 已覆盖 | `data.Xml.decodeWithoutErrorPtrAndEncodeData` | |
| `decodeXml(text, error*)` | DataModule.h | 已覆盖 | `data.XmlRoundTrip`, `data.XmlInvalid` | |
| `decodeXml(Data*, error*)` | DataModule.h | 已覆盖 | `data.XmlFromByteData` | |
| `encodeXml(doc, pretty)` | DataModule.h | 已覆盖 | `data.XmlRoundTrip` | |
| `encodeXmlData(doc, pretty)` | DataModule.h | 已覆盖 | `data.Xml.decodeWithoutErrorPtrAndEncodeData` | |

### 6.3 `ByteData` / `DataView` / `CompressedData`

| API | 头文件 | 状态 | 用例 | 缺口 |
|-----|--------|------|------|------|
| `ByteData` 构造 / `clone` / `getData` / `getSize` | ByteData.h | 已覆盖 | `data.ByteData.roundTrip`, `data.ByteData.sizeZeroThrows`, `data.ByteData.ownFalseCopies` | |
| `DataView` 构造 / `clone` / `getData` / `getSize` | DataView.h | 已覆盖 | `data.DataView.windowAndBounds` | |
| `CompressedData::getFormat` / `getDecompressedSize` / `clone` / Data 接口 | CompressedData.h | 已覆盖 | `data.compress.lz4RoundTrip` | |

### 6.4 `Compressor` / `HashFunction`

| API | 头文件 | 状态 | 用例 | 缺口 |
|-----|--------|------|------|------|
| `Compressor::getCompressor` | Compressor.h | 已覆盖 | `data.compress.invalidFormatThrows` | |
| `Compressor::compress` / `decompress` / `isSupported` | Compressor.h | 已覆盖 | 经模块 `compress`/`decompress` 间接 | |
| `HashFunction::getHashFunction` | HashFunction.h | 已覆盖 | `data.hash.md5KnownAndInvalid` | |
| `HashFunction::hash` / `isSupported` | HashFunction.h | 已覆盖 | 经模块 `hash` 间接 | |

### 6.5 `JsonDocument` / `XmlDocument`（类型 API）

| API | 头文件 | 状态 | 用例 | 缺口 |
|-----|--------|------|------|------|
| `JsonDocument::empty` / `isObject` / `isArray` / `object` / `array` / `root` | JsonDocument.h | 已覆盖 | `data.JsonDocument.queryBoundaries` + 既有 Json 用例 | |
| `XmlDocument::empty` / `get` / 解引用 | XmlDocument.h | 已覆盖 | 经 Xml 用例 + `encodeXmlData` | |

**data 覆盖进度：** 33 / 35 行「已覆盖」，2 行「部分」（encode/decode 非法 format 无独立抛错契约）。

## 7. API 明细：`filesystem`

现状摘要（2026-08-05 样板轮）：`test/filesystem_cpp.cpp` 共 8 个 C++ `TEST_CASE`（脚本冒烟不计入）。PhysFS 模式字符串为 `"rb"` / `"wb"` / `"c"`。回归 `--testcase=^filesystem\..*$` 全部 PASS。

### 7.1 `Filesystem`

| API | 头文件 | 状态 | 用例 | 缺口 |
|-----|--------|------|------|------|
| `init(arg0)` | Filesystem.h | N/A | | 单例构造已 init；禁止再调 |
| `setFused` / `isFused` | Filesystem.h | 已覆盖 | `filesystem.identityAndPaths` | fused 粘性：二次 `setFused(false)` 无效（实现契约） |
| `setupWriteDirectory` | Filesystem.h | 已覆盖 | 各写路径用例经 `useIdentity` | |
| `setAndroidSaveExternal` / `isAndroidSaveExternal` | Filesystem.h | 已覆盖 | `filesystem.identityAndPaths` | 桌面往返；无 Android 真机行为 |
| `setIdentity` / `getIdentity` | Filesystem.h | 已覆盖 | `filesystem.identityAndPaths` 等 | |
| `setSource` / `getSource` | Filesystem.h | 部分 | `filesystem.setSourceSecondCallFails` | 仅验二次失败；缺首次成功 + `getSource` 断言 |
| `mount(archive, …)` / `unmount(archive)` | Filesystem.h | 部分 | `filesystem.mount.missingReturnsFalse`, `filesystem.mount.realDirRoundTrip` | Win32 上 `/mnt/…` 内容 getInfo/read 未通过；仅验 mount/unmount 返回值 |
| `mount(Data*, …)` / `unmount(Data*)` | Filesystem.h | 未测 | | 需内存 zip；下轮补 |
| `newFile` | Filesystem.h | 已覆盖 | `filesystem.File.openReadWriteSeek` | |
| `newFileData` | Filesystem.h | 已覆盖 | `filesystem.FileData.metaAndClone` | |
| `getWorkingDirectory` / `getUserDirectory` / `getAppdataDirectory` | Filesystem.h | 已覆盖 | `filesystem.identityAndPaths` | |
| `getSaveDirectory` / `getSourceBaseDirectory` | Filesystem.h | 部分 | `identityAndPaths` 验 save | `getSourceBaseDirectory` 未单独断言 |
| `getRealDirectory` | Filesystem.h | 未测 | | |
| `getInfo` | Filesystem.h | 已覆盖 | `filesystem.writeReadAppendRemove` | |
| `createDirectory` / `remove` | Filesystem.h | 已覆盖 | `filesystem.writeReadAppendRemove` | |
| `read` / `write` / `append` | Filesystem.h | 已覆盖 | `filesystem.writeReadAppendRemove` | |
| `getDirectoryItems` | Filesystem.h | 已覆盖 | `filesystem.writeReadAppendRemove` | |
| `setSymlinksEnabled` / `areSymlinksEnabled` | Filesystem.h | 已覆盖 | `filesystem.identityAndPaths` | |
| `getRequirePath` / `getCRequirePath` | Filesystem.h | 已覆盖 | `filesystem.identityAndPaths` | |
| `allowMountingForPath` / `isRealDirectory` | Filesystem.h | 已覆盖 | `filesystem.mount.realDirRoundTrip` | |
| `getExecutablePath` | Filesystem.h | 已覆盖 | `filesystem.identityAndPaths` | |

### 7.2 `File`

| API | 头文件 | 状态 | 用例 | 缺口 |
|-----|--------|------|------|------|
| `open` / `close` / `isOpen` / `getMode` | File.h | 已覆盖 | `filesystem.File.openReadWriteSeek` | 模式为 `rb`/`wb`/`c` |
| `getSize` / `read` / `write` / `flush` / `isEOF` | File.h | 部分 | `openReadWriteSeek`, `bufferAndFlush` | `read(void*)` 内容已断言（勿对有副作用表达式用 `CHECK_EQ`）；`isEOF` 未测；`open("r")`→`rb` |
| `tell` / `seek` | File.h | 已覆盖 | `filesystem.File.openReadWriteSeek` | |
| `setBuffer` / `getBuffer` | File.h | 已覆盖 | `filesystem.File.bufferAndFlush` | 需在 open 前 setBuffer |
| `getFilename` / `getExtension` | File.h | 部分 | `openReadWriteSeek` 验 filename | `File::getExtension` 未测 |

### 7.3 `FileData`

| API | 头文件 | 状态 | 用例 | 缺口 |
|-----|--------|------|------|------|
| 构造 / `clone` / `getData` / `getSize` | FileData.h | 已覆盖 | `filesystem.FileData.metaAndClone` | |
| `getFilename` / `getExtension` / `getName` | FileData.h | 已覆盖 | `filesystem.FileData.metaAndClone` | `getName` 对 `folder/shot.png` 返回 `folder/shot`（仅按末点剥离） |

**filesystem 覆盖进度：** 约 28 / 40 行「已覆盖」；4「部分」；`mount(Data*)` / `getRealDirectory` 等仍「未测」；`init` 为 N/A。

## 9. API 明细：`event`

用例：`test/event.cpp`（+ `network.cpp` 中 `event.VariantMessage`）。回归含 `Event.*` / `event.*`。

| API | 状态 | 用例 / 备注 |
|-----|------|-------------|
| `Variant::make*` | 已覆盖 | `event.Variant.make*` |
| `Message` 构造 / args | 已覆盖 | `event.Message.withArgs`, `event.VariantMessage` |
| `push` / `poll` / `pollName` / `clear` | 已覆盖 | `event.pushPollRoundTrip`, `event.pollName*`, `event.clearThenPollEmpty`, `Event.*` |
| `pump` | 部分 | `event.pumpSmoke`（冒烟） |
| `wait` | 未测 | 阻塞 API；下轮补超时 harness |

## 10. API 明细：`mouse` / `touch`

**mouse**（`test/mouse_cpp.cpp`；脚本 `Mouse.basic` 不计入）：

| API | 状态 | 缺口 |
|-----|------|------|
| `isCursorSupported` / `setVisible`/`isVisible` / `isDown` | 已覆盖 | |
| `get/set Position/X/Y` | 已覆盖 | 绑定窗口句柄；无焦点时任意 warp 可能不更新 SDL 状态，测试回退 identity |
| `getSystemCursor` | 已覆盖 | 已知 + 非法抛异常 |
| `newCursor` / grab / relative | 未测 | newCursor 后端不完整 |

**touch**（`test/touch.cpp`）：`getTouches` 空列表、`getTouch` 无效 id → Exception → **已覆盖**。

## 11. API 明细：`window`

用例：`test/window_cpp.cpp`（14）。脚本 `WindowTest.*` 不计入。回归 `--testcase=^window\..*$`：**14/14 PASS**（2026-08-06 Window API 轮）。

| API | 状态 | 备注 |
|-----|------|------|
| `setWindowSettings` / `getWindowSettings` / `getHandle` | 已覆盖 | 320×240 往返；close 后重建；0×0 用桌面尺寸 |
| `setSize` / `getWidth` / `getHeight` | 已覆盖 | |
| `setFullscreenDesktop` / `setFullscreenExclusive` | 部分 | desktop 往返 + close 后 false；独占全屏无正例 |
| `close` / `isOpen` | 已覆盖 | |
| `setWindowTitle` / `getWindowTitle` | 已覆盖 | 含创建前缓存 |
| `setPosition` / `getPosition` | 已覆盖 | 容差 ±32 px |
| `setVSync` / `getVSync` | 已覆盖 | |
| `isVisible` | 已覆盖 | 与 title/position 同用例 |
| `minimize` / `maximize` / `restore` / `isMaximized` / `isMinimized` | 未测 | 下轮 |
| `hasFocus` / `hasMouseFocus` | 未测 | |
| `getPixelWidth` / `getPixelHeight` / DPI 换算 | 部分 | round-trip；`high_dpi` 未单独 smoke |
| `getDisplayCount` / `getDisplayName` / `getDisplayOrientation` / `getFullscreenSizes` / `getDesktopDimensions` | 已覆盖 | 含无效 index 边界 |
| `showMessageBox` / `showMessageBoxData` | 部分 | 默认跳过；`EVENGINE_TEST_MSGBOX=1` 手动 |
| `requestAttention` | 已覆盖 | no-crash |
| `setGraphics` | 部分 | 测试中调用，无独立断言 |
| `setWindowSettings`→false | 未测 | 难以稳定触发 |

## 12. API 明细：`image`

用例：`test/image.cpp`（10）。内存路径优先。

| API | 状态 | 备注 |
|-----|------|------|
| `newImageData(w,h)` / `setPixel`/`getPixel` / `inside` | 已覆盖 | |
| 非法尺寸 / 格式 / 垃圾 Data 解码 | 已覆盖 | |
| `isCompressed`（非压缩→false） | 已覆盖 | 正例压缩格式未测 |
| `clone` / `paste` / buffer own | 已覆盖 | |
| `encode` / `newCubeFaces` / 文件解码 | 未测 | 下轮 |

## 13. API 明细：`sound`

用例：`test/sound.cpp`（4）。

| API | 状态 | 备注 |
|-----|------|------|
| `newDecoder` / `newSoundData` / `newSoundDataEmpty` | 已覆盖 | 既有 + 非法 Data |
| `Decoder::seek/rewind/isFinished/clone` | 已覆盖 | |
| `newSoundDataFromDecoder` | 已覆盖 | |
| `newSoundData` 非法 Data | 部分 | 仅 Decoder 路径测了非法 |

程序化 demo 音效已迁至可选 `demo` 模块（`test/demo.cpp`）。

## 13b. API 明细：`model3d`（资源名 `3dmodel`）

用例：`test/model3d.cpp`（9）。CPU 解码 + 可选 GPU 渲染冒烟；上传见 `Graphics::newMeshFromAssimp` / `RenderSystem3D`。

| API | 头文件 | 状态 | 用例 | 缺口 |
|-----|--------|------|------|------|
| `Model3D::newModelData(Data*, hintExt)` | Model3D.h | 已覆盖 | `model3d.newModelData.objFromMemory`, `model3d.newModelData.emptyThrows`, `model3d.newModelData.rock1Fbx` | FileData 扩展名自动 hint 未单测 |
| `Model3D::newModelDataFromFile(path)` | Model3D.h | 已覆盖 | `model3d.newModelDataFromFile.missingThrows`, `model3d.newModelDataFromFile.sofaObjMtl` | |
| 渲染冒烟（ModelData → Mesh → RenderSystem3D） | — | 已覆盖 | `model3d.render.rock1Fbx`, `model3d.render.sofaObjMtl` | rock1 用 `Textures/Rock1_Diffuse.png` |
| `Model3D::getName` | Model3D.h | 已覆盖 | `model3d.Model3D.getName` | |
| `ModelData::getMeshCount` / `getVertexCount` / `getFaceCount` / `empty` / `getMesh` / `getScene` | ModelData.h | 已覆盖 | `model3d.newModelData.objFromMemory` | |
| `ModelData::getMaterialCount` / `hasNormals` / `hasTexCoords` | ModelData.h | 部分 | 间接经 obj 加载 | 非法 index 仅测了 getVertexCount |
| `ModelData` 非法 mesh index | ModelData.h | 已覆盖 | `model3d.newModelData.invalidMeshIndexThrows` | |

## 14. API 明细：`audio`

用例：`test/audio.cpp`（9）。无 OpenAL 时新用例 N/A skip。

| API | 状态 | 备注 |
|-----|------|------|
| `newSource` / play/stop / stream pump / spatial | 已覆盖 | 既有 |
| `stopAll` / `pause` / volume / pitch | 已覆盖 | |
| `Source::seek`/`tell` | 已覆盖 | 容差放宽（驱动漂移） |
| `newSourceFromDecoder` 等 | 部分/未测 | 见缺口 |

## 15. API 明细：`network`

用例：`test/network.cpp`（10 `network.*`）。

| API | 状态 | 备注 |
|-----|------|------|
| Tcp/Udp/Http/Channel 基础 echo | 已覆盖 | 既有 |
| `set/getTimeout` / `set/getVerifySsl`（Network） | 已覆盖 | |
| Tcp `isConnected` / `getLocalPort` | 已覆盖 | |
| Session add/get/remove/closeAll | 已覆盖 | |
| Http `setHeader` / `setBodyString` | 已覆盖 | |
| `HttpRequest::setVerifySsl` | 部分 | 会继承/覆盖并传入 submit；HTTPS 仍返回 `neterr`/`tls`（未链 Poco NetSSL） |

## 16. API 明细：`demo`（可选）

用例：`test/demo.cpp`（4）。需 `EVENGINE_BUILD_DEMO=ON`。

| API | 状态 | 备注 |
|-----|------|------|
| `newSound(music/shoot/explode/hit)` | 已覆盖 | |
| `newSound` 未知 kind 抛错 | 已覆盖 | |
| `newPlanetTexture` | 已覆盖 | 需窗口/Graphics |

## 17. `keyboard` / `joystick`（本轮 N/A）

详见 `.superpowers/sdd/module-kb-joy-report.md`。阻塞：未 `create_module`、未进 `EVELIBS`、无 `Module_REG`/`create()`、源码 `love::` 残留。接入构建后再展开 API 表。

## 18. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-08-05 | 初稿：规范 + 全模块总表 + `data`/`filesystem` API 展开与现状 |
| 2026-08-05 | 样板轮落地：`test/data.cpp` +13 用例；新建 `test/filesystem_cpp.cpp`（8 用例）；更新 §5–§7 进度；回归 `data`+`filesystem`+`FilesystemTest` 33/33 PASS |
| 2026-08-06 | 新增 `model3d`（`3dmodel`）：medialoader Assimp → `ModelData`；`test/model3d.cpp` 5 用例 PASS |
| 2026-08-05 | Round 2：event/mouse/touch/window/image/sound/audio/network/timer C++ 补测；keyboard/joystick 标 N/A；`EVTouch` 链入 unit_test；回归过滤 69 PASS；放宽 `audio.source.seekTell`；更新 §5 与 §9–§16 |
| 2026-08-05 | 修复：`File::read`/`open("r")` PhysFS 模式；Mouse WarpMouse 窗口句柄；Audio `stopAll` 野指针；HttpRequest verifySsl 继承（HTTPS 仍 stub `tls`）；补强相关断言 |
| 2026-08-06 | Window API 轮收尾：`Window.h` 范围 B 纯虚齐全；`test/window_cpp.cpp` 14 用例；`^window\..*$` 14/14 PASS；更新 §5 / §11 |
| 2026-08-06 | Demo 模块拆分：程序化音效/行星贴图迁出 Sound/Graphics；`EVENGINE_BUILD_DEMO` 可选编译 |
