这些都是可以载入的 module，不需要时可以不载入
全是等待被调用的模块，不会主动去调用

主要参考 love2d 和 Bevy 的设计，也将 cocos2dx 中的一些经典设计拿过来；游戏模型模板可参考 ECS 设计。

> 勾选约定：`- [x]` 表示该模块/能力已可用；`- [ ]` 表示待实现。可按子项逐步打钩推进。


## 类绑定原则

每个 API 都不重名，不使用重载，使用明确的类型，有部分参数有默认值。

枚举类型接口：可使用 `uint32_t` 或直接用 `string`。

计划使用 **string**，主要原因有 3：

1. 方便，无需定义枚举类型，少了很多绑定操作
2. 可扩展，日后 API 想扩展新功能，无需导出新定义，只需在函数中增加对新名称的支持
3. 直观，脚本与 C++ 代码中用 string 都很直观，避免宏、枚举引入头文件与 namespace 等问题


## 模块设计列表

### 1. 文件系统 — `filesystem`

封装核心文件系统：统一访问、目录查看、文件增删改查、监视文件变动。

- [x] 模块骨架与脚本绑定
- [x] 统一文件系统访问（physfs / 抽象层）
- [x] 目录查看、文件读写
- [ ] 文件增删改查完整 API
- [x] 监视文件变动（抽象 `watch`/`pollWatch`；physfs + Poco DirectoryWatcher）
- [ ] 打包/挂载 zip 等归档

### 2. 事件系统 — `event`

处理多线程信号同步、回调，以及来自硬件的输入信号。输入设备拆成独立子模块。

- [x] 事件队列、派发与回调注册（`push`/`poll`/`pushData`；脚本 `async_dispatch_event`）
- [x] 与多线程 / `thread` 的信号整合（`postMain` / `submitPost`）
- [x] 鼠标输入 — `mouse`
- [x] 键盘输入 — `keyboard`
- [x] 触屏输入 — `touch`
- [x] 手柄输入 — `joystick`

### 3. 窗口系统 — `window`

渲染窗口的设置、位置、样式、全屏等；仅支持单窗口（SDL 可覆盖当前需求）。

- [x] 模块骨架与 SDL 后端
- [x] 窗口创建、关闭、基本属性
- [x] 位置 / 尺寸 / 样式完整控制
- [x] 全屏 / 无边框 / 高 DPI
- [x] 与 `graphics` 的呈现表面对接

### 4. 图形系统 — `graphics`

核心渲染、显卡、shader。对外为**声明式**（ECS 组件 + C++ `RenderSystem`），而非脚本每帧命令式 draw。

**设计文档：** [2D渲染API设计.md](./2D渲染API设计.md)、[3D渲染管线.md](./3D渲染管线.md)（初始化与 VKBuilder、异步帧、buffers / 数据流 / 函数与 shader）。
架构：ECS.hpp 实体/组件 → `RenderSystem` → 内部 Batcher → VKBuilder。
Love2D 风格即时模式仅作 C++/DevTools 内部逃生舱。
分期：**A** = 下列「基础能力」；**B** = 「高级能力」。

**基础能力（A）：**

- [ ] ECS 整合（[ECS.hpp](https://github.com/sunxfancy/ECS.hpp) 子模块）与渲染组件（`Transform2D` / `Sprite`）
- [x] `RenderSystem`：每帧 View 收集并绘制（脚本不 per-sprite draw）
- [x] 纹理 / Quad 作为 Sprite 资源（image 解码 → GPU Texture）
- [ ] Vulkan 后端基础管线（VKBuilder Present / Pipeline / Texture）
- [ ] `clear` / `present`、与 SDL Window Surface 对接
- [x] 离屏 Canvas（组件或内部目标，P1）
- [x] 用户自定义 Shader（P1：`Shader` 资源 + `Sprite.shader` + `Graphics::newShader` / SPIR-V）

**高级能力（B，依赖 A，见设计文档第 8 节）：**

- [ ] 精灵表现扩展（帧/分层等）——帧动画已在 `animation` 模块（`SpriteAnim`）；分层见 Avatar
- [ ] 场景 / 调试用世界过滤
- [x] 地图类接口：TileLayer + tilemap
- [x] 摄像机：2D 光照与法线贴图
- [x] 拾取坐标：`Camera2D.screenToWorld*` / `worldToScreen*`；`Camera3D.screenToRay` + Math 射线检测
- [x] 纹理过滤：mipmap 链生成、LOD bias、各向异性过滤（`TextureCreateInfo` / `TextureSampler`）
- [x] 几何 LOD：`Renderable3D::setMeshLod` 按相机距离切换 Mesh
- [x] 低像素优化：小图旋转等（`ImageData::rotate`：nearest / linear / **RotSprite**）
- [x] 简单 3D model 支持（`Model3D` 加载 + `Graphics::newMeshFromAssimp` / `RenderSystem3D`）
- [x] 体素实例化矩形（`Graphics::drawVoxelFaceInstances` + `voxel` 模块）
- [x] 3D 多光源 + PBR（`Light3D` / metallic-roughness / 法线贴图前向）
- [x] 3D Clustered Forward 多光源（CPU 分簇 + SSBO，>8 灯自动切换）
- [x] 3D IBL / Environment 反射（`Camera3D` env cubemap + specular Fresnel）
- [x] 3D Directional CSM 阴影（单方向光 3 cascade + PCF）
- [x] 纹理 Cell Bombing（`Renderable3D::setTexCellBomb`：随机 cell 偏移/旋转混合，消除平铺重复）
- [x] 视差遮蔽贴图 POM（`Renderable3D::setHeightTexture` + `setParallax`：高度图 R 通道驱动 UV 偏移）
- [x] 屏幕空间体积光（`Volumetric`：God Rays + 尘雾；质量档 low/medium/high；Drawable 遮挡）
- [x] 体积光 Ray March（线性深度 + 屏空间遮挡近似 CSM；`setMode("raymarch")`）
- [x] 体积雾（高度指数 + 距离 ramp；`setMode("fog")` / `applyFog`）
- [x] 屏幕空间环境光遮蔽（`AmbientOcclusion`：SSAO / HBAO / GTAO + 双边模糊 + overlay + 3D `applyFromDepth`）
- [x] 经典抗锯齿（`AntiAliasing`：FXAA / SMAA / SSAA / NFAA；3D 经 scene color 自动 FXAA，`RenderControl "aa"` 默认开）
- [x] 屏幕空间全局光照（`GlobalIllumination`：SSGI 采样 lit scene color + mesh 半球/wrap；`RenderControl` `"ao"`/`"gi"`/`"aa"` 默认开）
- [x] `Material` + `ModelPart`（着色参数打包，挂到模型部件）
- [x] `RenderControl`（特性编译为 Pass：shadow / gbuffer / forward / hair）
- [x] 可采样 GBuffer（线性深度 + 世界法线，可选 albedo；HW z-test 仍用于前向）
- [ ] （可选）Planar Reflection / Point Shadow / SSR
- [ ] （可选）体积雾 Froxel 网格 / 后处理绑定真实 CSM shadow map
- [ ] （可选）物体空间 AO / VXAO / RTAO
- [ ] （可选）硬件 MSAA / 完整多 Pass SMAA LUT / TAA

### 5. 数据抽象 — `data`

二进制数据、压缩、哈希、视图等底层数据工具（资源与网络等模块的基础）。

- [x] 模块骨架
- [x] ByteData / DataView
- [x] 压缩（Compressor）与哈希
- [x] 与资源热重载 / cache 的统一数据接口

### 6. 资源管理系统

负责加载图形、声音、视频、字体、3D 模型、动画等不同格式（优先 image / sound，其次 fonts / 3dmodel）。

- [x] 图片资源 — `image`
- [x] 音频资源 — `sound`
- [x] 立体声播放 — `audio`（OpenAL）
- [ ] 视频资源 — `video`
- [x] 字体资源 — `font`（`eve.Font` / FreeType → `FontData`）
- [x] 本地化 — `i18n`（`eve.I18n`：JSON 翻译表 / 点号键 / 占位符 / 复数规则 / 默认语言回退 / 热重载）
- [x] 3D 模型资源 — `3dmodel`（`model3d` / `eve.Model3D`，medialoader Assimp → `ModelData`）
- [ ] 动画资源载入（与 `animation` 配合）
- [ ] 统一 cache、引用计数与热重载标记

### 7. 插件系统 — `plugins`

加载第三方模块与插件（dll 等）。核心是一套基于 string 的依赖管理系统（可由原 PluginFramework 思路迁移）。

- [x] 动态库加载（dll / so）
- [ ] 基于 string 的依赖声明与解析
- [ ] 插件生命周期（加载 / 卸载 / 热替换）
- [ ] 与引擎 module 注册表对接

### 8. 管理系统 — `system`

系统版本、时间、状态等查询与设置。脚本：`system <- eve.System()`。

- [x] 系统信息（OS、CPU、内存、进程内存）
- [x] 墙钟时间 / 休眠（帧计时仍用 `Timer`）
- [x] 剪贴板
- [x] 电量状态
- [x] GPU 名称 / 厂商 / 类型 / 显存总量（需 Graphics 已 init）

代码：[`src/modules/system/`](../../src/modules/system/)

### 9. 网络系统 — `network`

封装网络通讯基础操作；Poco 已有较好实现，需考虑异步事件并整合进 `event`。

- [x] TCP / UDP 基础封装
- [x] HTTP / 简单客户端（可选）
- [ ] 异步 IO 与 `event` 整合
- [ ] 错误处理与超时

### 10. 线程系统 — `thread`

线程、线程池、异步任务；必要时可考虑 TBB；注意与 `event` 整合。

- [x] 线程池 / Task 提交（`eve.Thread` → `ThreadPool` / `Task`）
- [x] Channel / 跨线程消息（`ThreadChannel`；命名 `getChannel`）
- [x] 与 `event` 的跨线程投递（`postMain` / `submitPost`；Event 队列已加锁）
- [x] Squirrel 异步运行时（`async.nut`：Promise / nextTick / setTimeout / `async_pump`）
- [ ] 独立脚本线程（Squirrel VM 非线程安全，暂不支持）

代码：[`src/modules/thread/`](../../src/modules/thread/)、[`src/scripts/async.nut`](../../src/scripts/async.nut)
脚本：`thread <- eve.Thread()`；帧循环调用 `async_pump()`（见 `load.nut`）。
注意：worker 内不要调用脚本 / graphics；用 Channel 或 `postMain` 回主线程，再 `Promise.then`（`.fail` 代替 JS `.catch`）。
### 11. GPU 计算系统 — `gpgpu`

并行与异构计算，封装 compute shader；计算结果 buffer 尽量直接交给 `graphics`。脚本：`gpgpu <- eve.Gpgpu()`。复用 Vulkan `Graphics` 已建好的 device/queue（无独立 headless 计算设备路径，需先创建窗口）。

- [x] Compute shader 封装（`Gpgpu::newShader` glslc 编译 / `newShaderFromSpvFile`；固定 8 个 SSBO binding + 32 float push constant）
- [x] `GpuBuffer`（storage / staging）+ `writeData`/`readData`/`writeFloat32`/`readFloat32`/`writeFloat32s`；与 `graphics` 共用同一 `vkb::Device`
- [x] 供 `tensor` 消费：`CompiledFunction` 图执行 + 大张量 reduce（见 17b）
- [x] ECS 桥接：`ShaderSystem` / `EcsGpu.h`（C++ View 打包）+ `eve.ShaderSystem`（脚本 System 用 GLSL 编写）+ `packEcsFloats`/`unpackEcsFloats`
- [ ] 与 `thread` 任务调度协调（可选）

### 12. 粒子系统 — `particles`

创建、渲染、加载与配置粒子效果。脚本配置 `ParticleEmitter` 组件；每帧由 ECS System 驱动。

- [x] 粒子发射器与生命周期（ECS `ParticleEmitter`：Config / Sim / Draw / Resource / Attach / SkinSource）
- [x] `ParticleSimSystem::update` + `ParticleRenderSystem::render`（与 Module 解耦）
- [x] 与 `graphics` 的批次渲染（`Camera2D` / `Canvas` 对齐 `RenderSystem`）
- [x] 发射区域（point / ellipse / rect）、径向·切向加速度、自旋与尺寸抖动
- [x] 外观增强：加法/不透明混合、精灵表序列帧（flipbook）、多段颜色渐变与大小/旋转生命周期曲线、初始随机旋转
- [x] 粒子渲染并入 `DrawItem2D` 统一路径（旋转、UV、混合、layer 排序、Camera 一次到位）
- [x] 发射控制：定时爆发（bursts）、one-shot（emitterLife）、预热（prewarm）、line/ring 发射区域
- [x] 受力：重力、阻尼、限速、速度生命周期曲线、继承发射器速度、本地/世界仿真空间、CPU 值噪声湍流
- [x] 碰撞：世界边界与可插拔解析器（kill/bounce/stop + 弹性 + 寿命损失）
- [x] 子发射器（birth/death/collision 触发，可继承速度）与拖尾（velocity-stretched）
- [x] 健壮性：buffer 溢出策略（drop/pause/warn）、单帧步长上限、带相机发射器屏外跳过模拟
- [x] 径向力场（attract/repel + 衰减指数）、自定义粒子 Shader、粒子灯光（Light2D 池，`ParticleLightSystem`）
- [x] GPU 加速模拟（计算着色器积分 + SSBO + CPU 回读渲染，`stepEmitterSimGpu`，无设备自动回退 CPU；内核见 `ParticleGpuKernel.h`）
- [x] 基础预设配置（`applyPreset`: spark / smoke / fire）
- [x] 资源文件配置加载与热更新（JSON + `Resource` + `ParticleConfigSystem::poll`）
- [x] 动态骨骼绑定（`attachToBone` / `attachToBoneByName` / `attachToSpineBone*` / `attachToSkeleton2D` / `attachToSkeleton3D`；plane+scale 投影；可选跟随骨骼朝向）
- [x] 蒙皮表面发射（`setSkinSource` / `emitFromSkin`，可选按骨权重过滤顶点）

代码：[`src/modules/particles/`](../../src/modules/particles/)
示例配置：[`example/particles/fire.json`](../../examples/basic/particles/fire.json)、[`example/particles/spark.json`](../../examples/basic/particles/spark.json)
帧序约定：动画 `computeWorld` / Spine `updateWorldTransform` / IK `forwardKinematics|solve` → `ParticleConfigSystem` →（骨骼/蒙皮 sync）→ `ParticleSimSystem` →（可选 `RenderSystem`）→ `ParticleRenderSystem` → UI → present。
脚本：`particles.update(dt)`（含 poll+sim+attach sync）与 `particles.render(gfx)`；见 `src/scripts/load.nut` / `example/main.nut`。
相关测试：[`test/particles.cpp`](../../test/particles.cpp)、[`test/particles_attach_skin.cpp`](../../test/particles_attach_skin.cpp)、[`test/particles_dynamic_bones.cpp`](../../test/particles_dynamic_bones.cpp)、[`test/particles_attach_more.cpp`](../../test/particles_attach_more.cpp)、[`test/particles_attach_extra.cpp`](../../test/particles_attach_extra.cpp)。
**JSON 字段（对象根）：** `buffer`（仅创建时）、`preset`、`x`/`y`、`emissionRate`、`particleLifetime`/`lifeMin`·`lifeMax`、`emitterLife`、`direction`、`spread`、`speed`、`linearAcceleration`、`radialAcceleration`、`tangentialAcceleration`、`emissionArea`（`{type,x,y}`，type 支持 none/ellipse/rect/line/ring）、`particleSize`、`sizes`、`sizeVariation`、`spin`、`startRotation`、`bursts`、`prewarm`、`gravity`、`damping`、`limitVelocity`、`velocityOverLifetime`、`inheritVelocity`、`simulationSpace`、`noise`（`{strength,frequency,speed}` 或根级 `noiseStrength` 等）、`collision`（`{mode,radius,restitution,lifetimeLoss}`）、`collisionBounds`（`{enabled,minX,minY,maxX,maxY}`）、`worldCollision`、`renderMode`/`stretch`、`overflowMode`、`maxDeltaTime`、`gpuSimulation`、`forceFields`（`[{x,y,radius,strength,falloff}]`）、`lights`（`{enabled,max,radius,intensity,color}`）、`blendMode`、`flipbook`（`{hframes,vframes,frameRate,frameRandomStart}`，或根级 `hframes`/`vframes`/`frameRate`/`frameRandomStart`）、`colorOverLifetime`、`sizeOverLifetime`、`rotationOverLifetime`、`colorStart`/`colorEnd`、`layer`、`visible`、`texture`、`autoStart`、`autoReload`。

### 12b. 内置演示 — `demo`（可选）

仅服务默认 meteor-defense 演示的程序化资源；**非**通用游戏 API。CMake `EVENGINE_BUILD_DEMO=ON`（默认）时编译并链入 `EVDemo`；`OFF` 时不编译、不嵌入 `demo.nut`。

- [x] `Demo::newSound(kind)` — `music` / `shoot` / `explode` / `hit`
- [x] `Demo::newPlanetTexture(gfx)` — 等距柱状行星 albedo
- [x] 嵌入脚本 `demo.nut` + `load.nut` 无 `main.nut` 时按需运行

通用能力仍在核心模块：`Sound::newSoundDataEmpty`、`Graphics::newMeshSphere` / `newTexture` / `render3D` 等。

代码：[`src/modules/demo/`](../../src/modules/demo/)

### 13. 物理 — `box2d` / `box3d` / `Physics`

管理 Box2D（2D）与 Box3D（3D）刚体世界，以及可交互的 2D 布料与粒子流体。2D 为 LÖVE 风格像素坐标 + `setMeter`；3D 为米制坐标。

- [x] World / Body / Fixture 封装（Box2D）
- [x] World3D / Body3D / Shape3D 封装（Box3D：box / sphere / capsule）
- [x] 与 game loop 的 step 同步（`World::update` / `World3D::update`）
- [x] 碰撞回调接入 `event`（2D：`begincontact` / `endcontact`；3D：`begincontact3d` / `endcontact3d`）
- [x] Debug draw（可选，`World::drawDebug(gfx)`，2D）
- [x] 查询拾取：`rayCast` / `queryAABB` / `testPoint`（2D 像素；3D 米）
- [x] 可交互布料：`Cloth`（Verlet + 距离约束，`grabAt` / `pin` / `draw`）
- [x] 可交互 2D 流体：`Fluid`（双密度松弛，`emit` / `interactAt` / `draw`）

脚本入口：`physics <- eve.Physics()`；`world <- physics.newWorld(gx, gy)`；
`world3 <- physics.newWorld3D(gx, gy, gz)`；`cloth <- physics.newCloth(...)`；`fluid <- physics.newFluid(capacity)`。

代码：[`src/modules/physics/`](../../src/modules/physics/)
示例：[`examples/softbody/`](../../examples/softbody/)
用户文档：[`docs/usr/modules/physics.md`](../usr/modules/physics.md)

### 14. 动画系统 — `animation`

补间动画（属性插值）已落地；2D 帧动画与 Spine（region 子集）已落地；3D 骨骼播放支持状态机与 Motion Matching 两套方案。

脚本：`anim <- eve.Animation()`；类型 `Tween` / `SpriteSheet` / `SpriteClip` / `SpriteAnim` /
`SpineAtlas` / `SpineSkeletonData` / `SpineSkeleton` / `SpineAnim` /
`AnimSkeleton` / `AnimClip` / `AnimPose` /
`AnimPlayer` / `AnimStateMachine` / `MotionDatabase` / `MotionMatcher` /
`ControlAnim` / `ControlPose` / `AnimTrail`。

- [x] 属性补间（`setFrom` / `setTo` / **`setDelta` 相对差值**）
- [x] 多属性并行、缓动（与 `Math.ease` 同名 kind）、delay / repeat / yoyo
- [x] 角度最短路径（`setToAngle` / `setDeltaAngle`）
- [x] 模块泵 `anim.update(dt)` 或单 tween `update(dt)`
- [x] 帧动画（2D sprite sheet：`SpriteSheet` / `SpriteClip` / `SpriteAnim`）
- [x] Spine 动画格式（`.atlas` + skeleton JSON region 子集：`SpineAtlas` / `SpineSkeletonData` / `SpineSkeleton` / `SpineAnim`）
- [x] 3D 骨骼动画：`AnimSkeleton` + `AnimClip` 采样 + `AnimPose` FK
- [x] 播放器：`AnimPlayer`（play / crossFade）
- [x] 状态机方案：`AnimStateMachine`（float/bool/trigger 条件、exit time、交叉淡入）
- [x] Motion Matching：`MotionDatabase.bake` + `MotionMatcher`（轨迹/速度/姿态代价搜索）
- [x] 控制论程序动画：`ControlAnim` / `ControlPose`（二阶动力学 / 闭式阻尼弹簧 / PD）
- [x] 拖尾轨迹：`AnimTrail`（采样点 / 骨骼投影、时长淡出、`draw(gfx)`）
- [x] Assimp/`ModelData` 导入与紧凑 `.eva` 夹具（Mixamo Idle/Run/Jump 集成测试）
- [x] 蒙皮数据查询（`ModelData` bone/weight/inverse-bind）+ CPU 线性混合蒙皮（`AnimSkin`）
- [x] 免费蒙皮角色集成测试（Khronos CesiumMan，构建时 `download_skinned_character` 下载）
- [x] 与精灵 / Avatar 分层渲染对接（Avatar Image 层走 Renderable2D；动作名 `setMotion`）
- [ ] 动画资源热重载；更多 locomotion 库
- [ ] Spine mesh/IK/path 全量运行时（可走官方 spine-cpp 插件）
- [ ] 与 scene / GPU 蒙皮网格自动同步（后期）

### 14b. 逆运动学 — `ik`

基于 [ik.hpp](https://github.com/sunxfancy/ik.hpp)（`external/ik.hpp`）的独立 IK 解算模块。FABRIK + 关节角约束 + 可选 force 软求解；与 `animation` 解耦，只负责链式骨骼姿态解算。

- [x] 子模块接入与 CMake include
- [x] `Skeleton2D` / `Skeleton3D`（建骨、FK、bind、约束）
- [x] `Solver2D` / `Solver3D`（target / solve / step）
- [x] Squirrel 导出：`ik <- eve.IK()`
- [ ] 与 scene / 3D 骨骼蒙皮对接（后期）

脚本示例：

```squirrel
ik <- eve.IK()
sk <- ik.newSkeleton2D()
b1 <- sk.createBone(0, 1.0)
b2 <- sk.createBone(b1, 1.0)
sk.initStraightPose(0, 0)
solver <- ik.newSolver2D()
solver.setMaxIterations(32)
solver.addTarget(b2, 1.0, 1.0)
solver.solve(sk)
```

代码：[`src/modules/ik/`](../../src/modules/ik/)
代码：[`src/modules/animation/`](../../src/modules/animation/)

### 15. Tilemap 地图 — `map`

地图读写、多层表示；C++ 侧直接生成/访问 array，便于 squirrel 访问；有资源时可作 renderable。

- [x] 多层地图数据结构（`TileLayer` ECS）
- [x] 读取 Tiled JSON（含 base64+zlib/gzip；未知 orientation 失败；zstd 不支持）
- [x] 投影：orthogonal / isometric / staggered / hexagonal + `tileToWorld` / `worldToTile` / `depthY`
- [x] 对象层解析（`Map` 对象缓存 API）
- [x] 与精灵统一 2D 深度队列（`Map::render` → `DrawItem2D`）
- [x] Squirrel 侧友好访问
- [x] Dual-grid 解算（逻辑层 → 半格偏移显示层，15-tile；正交/等距/交错/六角；Tiled 无原生 dual-grid）
- [x] 格子寻路：`Pathfinder`（A\*）+ 拓扑 `ortho4` / `ortho8` / `hex`（适配正交/等距/交错/六角）
- [x] 群体寻路优化：同目标 `FlowField`（一次 Dijkstra，多单位 `followFlow` / `findGroupPath`）
- [x] 动态视野 / 战争迷雾：`Fov`（Phase A–C：2D/3D、hex、rectangle、感知检测、GPU 遮罩纹理）
- [ ] 与地图编辑器联动（消费 `editor` 笔刷构件；DevTools 可选）

**寻路设计文档：** [寻路系统设计.md](./寻路系统设计.md)  
**动态视野设计文档：** [动态视野系统设计.md](./动态视野系统设计.md)

### 15b. 程序化生成 — `procgen`

运行时程序化内容生成（一期地图，二期贴图）。算法注册表 + 可配置输出；与 `map`/`math` 配合，不替代地图表示与渲染。

**设计文档：** [程序化生成模块设计.md](./程序化生成模块设计.md)

- [x] 模块骨架与脚本绑定（`eve.Procgen`）
- [x] `Params` / `Grid2D` / `OutputSpec` / `GeneratorRegistry`
- [x] 内置地图算法：`dungeon.bsp` / `cave.cellular` / `cave.drunkard` / `maze.backtrack` / `noise.terrain` / `wfc.simple`
- [x] palette 语义 id → GID + `applyToLayer`
- [x] 可配置输出：`grid`（`generate`）/ `tilelayer` / `json`；（`png` 待定）
- [ ] 用户扩展：C++ 注册 / 插件；脚本算法（可后置）
- [x] （二期）贴图 recipe：`tex.soil` / `tex.stone` / `tex.marble` / `tex.water` / `tex.sky_cloud` + normal
- [x] （网格）`mesh.marchingcubes` → `buildMesh` / `generateMesh` + `Graphics::newMeshFromArrays`
- [x] （网格）`mesh.hexplanet` / `mesh.rock` / `mesh.tree` / `mesh.skyscraper`（程序化 3D 网格 recipe，同 seed 可复现）

### 15c. 风格化渲染 — `stylize`

NPR / 风格化后处理与网格着色：卡通、手绘水彩、水墨、像素。复用 `graphics` 的 `Shader` + `Canvas` + `drawTexturedRectShader`；3D 卡通复用 `mesh3d_toon` SPIR-V。

**设计文档：** [风格化渲染模块设计.md](./风格化渲染模块设计.md)

- [x] 模块骨架与脚本绑定（`eve.Stylize` / `StylePass` / `StyleChain`）
- [x] 风格 id（string）：`cartoon` / `watercolor` / `ink` / `pixel`
- [x] 后处理 `StylePass`：`newPass` / `newPassFromShader` / `apply` / `applyTo`
- [x] 多 Pass：`StyleChain`（ping-pong，为可分离模糊 / 描边链预留）
- [x] 能力查询：`supports(style, feature)`（`gbuffer` 对接 RenderControl 深度/法线）
- [x] 网格着色：`newMeshShader`（`cartoon` / `ink`）
- [x] CPU `processImage`（RGBA8，测试与离线工具）
- [x] 预编译 SPIR-V（`scripts/compile_stylize_shaders.py`）
- [x] `Material`：打包着色模型 + 贴图/PBR，可挂到 `Renderable3D` / `ModelPart`
- [x] `RenderControl`：特性开关编译为有序 Pass（shadow → gbuffer → forward → hair；`ao`/`gi`/`aa` 默认开）
- [x] GBuffer 深度/法线（线性深度 R + 编码法线），可选 albedo
- [ ] （后续）GBuffer 驱动风格描边、风格动态注册、可分离渗色多 Pass 实现

代码：[`src/modules/stylize/`](../src/modules/stylize/)

### 16. 数据库 — `database`

通过 third-party POCO Data 连接 SQLite；实体设定后支持 ORM，便于游戏数据更新与查询。完整用法见[数据库模块设计](./数据库模块设计.md)。

- [x] SQLite 连接、原始 SQL 与基础 CRUD
- [x] 显式 `Mapping<T>` ORM 映射
- [x] ECS View 批量事务导出
- [x] Squirrel JSON 行接口
- [ ] 热重载时的数据一致性策略

### 17. 数学库 — `math`

矩阵、向量；导出部分 glm 操作，并实现 noise、贝塞尔曲线等。脚本：`math <- eve.Math()`。

- [x] 向量 / 矩阵基础导出（`Vec2` / `Vec3` / `Mat4`）
- [x] 常用几何与变换工具（length/distance/dot/normalize/pointIn* / rotate / polar）
- [x] 2D/3D 拾取与碰撞检测几何（`*Overlap` / `segmentsIntersect` / `raycast*` / `pointInSphere|Box` / `closestPointOnSegment*`）
- [x] Noise（simplex/perlin + `fbm` / `ridged` / `turbulence` / `voronoi` / `warpNoise`）
- [x] 贝塞尔曲线（二次 / 三次，1D 与 2D）
- [x] 随机数工具（可种子 `mt19937` + Gaussian）
- [x] 程序化生成辅助（`hash` / `ease` / `bias`/`gain` / `snap` / `bilinear`）

代码：[`src/modules/math/`](../../src/modules/math/)

### 17a. 空间索引 — `spatial`

地图 / 场景元素的宽相空间查询与裁剪候选（与 `procgen` 地牢 BSP **无关**）。脚本：`spatial <- eve.Spatial()`。

**设计文档：** [空间索引模块设计.md](./空间索引模块设计.md)

- [x] 模块骨架与脚本绑定（`eve.Spatial`）
- [x] `QuadTree`（2D）/ `Octree`（3D）
- [x] `SpatialHash2D` / `SpatialHash3D`
- [x] `BSPTree2D` / `BSPTree3D`（kd 风格 AABB 二分）
- [x] 统一查询：`queryPoint` / `queryRect|queryAABB` / `queryCircle|querySphere` + `getResult*`
- [ ] （可选）与 `map` 对象层 / 视口裁剪适配器、`queryFrustum`

代码：[`src/modules/spatial/`](../../src/modules/spatial/)

### 17a1. 编辑器构件 — `editor`

引擎不附带完整 3D/2D 编辑器，提供组装自定义编辑器的构件（gizmo / 笔刷 / UI 壳）。  
**设计文档：** [编辑器模块设计.md](./编辑器模块设计.md)。脚本：`editor <- eve.Editor()`。

- [x] 模块骨架与脚本绑定（`eve.Editor`）
- [x] `TransformGizmo`：translate / rotate / scale / bound；local/world；snap；射线拖拽；`getPart*` 绘制描述
- [x] `GizmoManager`：多 mode 开关、attach/detach、统一 pick/drag（Babylon 风格）
- [x] `TileBuffer` + `Brush`：paint / erase / fill / line / rect / stamp；preview / change 列表
- [x] `EditorToolbar` / `EditorInspector` / `EditorDock` / `EditorHistory`
- [x] `test/editor.cpp` + 用户文档
- [ ] （可选）与 `scene` / `map.TileLayer` 薄适配、多选变换、ImGui 即时绘制后端

代码：[`src/modules/editor/`](../../src/modules/editor/)

### 17a2. 体素渲染 — `voxel`

高性能 32³ chunk 体素：贪婪矩形合并、32-bit 打包实例、六向子缓冲、视锥/距离/朝向裁剪后实例化绘制。脚本：`voxel <- eve.Voxel()`。

**设计文档：** [体素渲染模块设计.md](./体素渲染模块设计.md)

- [x] 模块骨架与脚本绑定（`eve.Voxel` / `VoxelWorld` / `VoxelChunk`）
- [x] 32³ chunk + 六向 `PackedRect` instance buffer
- [x] 贪婪 meshing（暴露面 → 矩形）
- [x] 32-bit 打包：`xyz(5×3) + wh(5×2) + tex(7)`
- [x] `selectVisible`：视锥 + 视距 + 面朝向
- [x] `Graphics::drawVoxelFaceInstances`（Vulkan 实例化 unit-quad）
- [ ] （可选）跨 chunk 邻接消隐、增量 remesh、LOD

代码：[`src/modules/voxel/`](../../src/modules/voxel/)

### 17b. 张量计算 — `tensor` / `TF`

接近 TensorFlow 2：命名空间 `tf <- eve.TF()`，值类型 `Tensor`。默认 eager；`tf.func()`（对应 `tf.function`）追踪计算图后 `compile` / `run`。

- [x] Phase 1 CPU eager：`zeros`/`ones`/`fill`/`eye`/`arange`/`randomUniform` 等
- [x] 模块级 op：`add`/`multiply`/`matmul`/`relu`/`reduceSum`/`where`/`reshape*`…
- [x] `tf.func()` → `Func`：`input*` / `setOutput` / `compile` → `CompiledFunction.run*`
- [x] Symbolic `Tensor` 禁止读值；eager 常量在追踪中自动捕获为 `Const`
- [x] CPU 拓扑执行 + 逐元素 unary 链融合；`getDevice()` → `"cpu"`
- [x] Phase 2：`CompiledFunction` 经 `gpgpu` compute shader 执行（逐元素二元/一元、`matmul`、`transpose`、`where`；`Reshape`/`Flatten` 走 buffer 别名）；编译时探测 Vulkan/`glslc` 可用性，不可用则回退 CPU，`getDevice()` → `"gpu"`（见 `src/modules/tensor/GpuBackend.cpp`）
- [x] 大张量 `reduceSum`/`reduceMean`/`reduceMin`/`reduceMax`（eager）在可用时走 GPU 并行规约，小张量仍用 CPU（阈值 16384 元素）
- [ ] 图内 dispatch 批量录制（目前每节点单独同步提交，尚未合并为单次 command buffer + barrier）

代码：[`src/modules/tensor/`](../../src/modules/tensor/)

### 18. 实体系统 — `entity`

基于 [ECS.hpp](https://github.com/sunxfancy/ECS.hpp)（`external/ECS.hpp`），支持类继承式 Entity 与 `View` 遍历；替换原未完成的自研 ECS。与声明式渲染共用同一套实体表（见 [2D渲染API设计.md](./2D渲染API设计.md)）。

- [x] 引入 ECS.hpp 子模块，CMake include，替换 `common/ECS.h` 集成层
- [x] C++ 基础用例（`test/ECS.cpp`：create / component / View 含继承）
- [x] 与脚本侧对象模型对接（`exposeECS`：`eve.Component` / `eve.Entity` / `eve.EntityContainer` / `eve.System` / `eve.view`）
- [x] GPGPU System：`eve.ShaderSystem` + `gpgpu::ShaderSystem` / `EcsGpu.h`（用 compute shader 编写批量 System）
- [ ] 状态刷新 / 依赖隔离钩子
- [x] 与 snapshot 对接（`eve::dev::Snapshot` 序列化脚本根；引擎侧无状态约定）
- [ ] 与正式存档系统对接（持久化格式 / 版本迁移）
- [ ] `entity` 模块封装（可选，对上游薄封装）

### 19. 声明式 GUI — `ui`

在 ImGui 之上的保留模式游戏 UI（React 式 `build` + dirty，挂 ECS `UIHost`）。脚本不每帧调用 ImGui。
**设计文档：** [GUI框架设计.md](./GUI框架设计.md)。

- [x] imgui 静态库接入（主工程 `eve_imgui` + third-party CMake）与 SDL/Vulkan backend
- [x] `UIHost` / `Tree` + `UISystem`（Window / Text / Button）
- [x] Graphics present overlay 钩子
- [x] 脚本 `mountSimple` / `setLabelText` / `setButtonSetsLabel`（Component 完整形态待 B）
- [x] 与 boot 帧序对接（`load.nut`：`UISystem` → `present` → `dispatchEvents`）
- [x] B：List / 局部 dirty / 主题 / C++ Component.build（脚本 Component 类待续）
- [ ] C：DevTools 反射属性面板

### 20. 声明式场景树 — `scene`

与 `ui` **同构**：`SceneComponent.build() → NodeDesc → SceneHost`，`mount` / `remountReconcile` / key patch；场景即函数（`props` + `children` 插槽），不做 Prefab/SceneTemplate。

| UI | Scene |
|----|-------|
| `WidgetDesc` | `NodeDesc` |
| `UIHost` + `UINode` | `SceneHost` + `SceneNode`（概念上即 GameObject） |
| `Component` | `SceneComponent`（脚本 `eve.SceneComponent`） |
| `applyTree` / reconcile | 同名同逻辑 |
| — | `TransformSystem`（local TRS → world Mat4） |

- [x] `SceneHost` / `NodeDesc` / `applyTreeReconcile`
- [x] `SceneComponent`（build / dirty / mountAs）
- [x] `TransformSystem` + 命令式 `setParent`（环检测）
- [x] Module `eve.Scene` + C++ 单测 `test/scene.cpp`
- [x] 与 `Renderable2D/3D` 的可选 Link 同步（world TRS → transform；rebuild 按 id 保留）
- [x] 脚本 builder（`beginNode`/`mountBuildAs`）+ `eve.SceneComponent`
- [x] 树查询：path / parent-children / DFS·BFS / `findIf`·`filter` / collectIds*
- [x] 节点 ↔ 脚本 ECS 打通：懒创建 `SceneObject` 实体 + `eve.SceneEntity`（attach/detach/update、`eve.view` 可见）+ `eve.SceneNodeRef` 节点句柄；reconcile/rebuild 按 id 保留绑定
- [x] 脚本 ECS 性能：view 缓存（create/destroy 失效、C++ 桥接类不缓存）、O(1) 销毁、槽与组件默认值缓存
- [x] 通用 link 系统：节点多 link 槽（renderable2d/3d、physics2d/3d 双向、camera3d、audio3d），按 id 保留，脚本 + NodeRef API
- [x] 脚本 API 补齐：变换 getter/world、local↔world、层级操作（setParent/removeNode/addChild）、四元数/lookAt、节点 tag/layer + `collectIdsByTag`、id 唯一性校验、link 悬垂自动摘除
- [x] 增量变换更新：`subtreeDirty` 子树裁剪（API 编辑只重算脏子树 + 只同步受影响 link；全净树零开销；直接字段写仍走全量兜底）
- [x] id→index 哈希索引：`findById/findIndexById` 惰性重建 O(1) 查找
- [x] reconcile 同父移动/排序：key 集合匹配 + 递归重排，不动 arena，link/脚本绑定保留
- [x] 节点生命周期事件：`node_added/node_removed/node_moved/node_changed`，C++ `SceneHost::setEventHandler` + 脚本 `scene.setNodeEventHandler(cb)`；`SceneComponent.onMount`（C++ + 脚本）
- [x] 主循环接入：默认 `eve_update` 调用 `scene.updateTransformsAll()`（全净树零开销）
- [x] 错误语义：新 getter 对不存在节点抛 Squirrel 异常（`scene.hasNode` 可前置守卫）
- [x] 场景序列化：`serializeHost/deserializeHost`（SceneHost ↔ JSON，含 TRS/tags/layer/bounds/children，Poco JSON）
- [x] 节点拾取与裁剪：节点 AABB + `pickRay/pickScreen`（射线/屏幕拾取）、`collectFrustumIds`（相机视锥）、`syncSpatialIndex` + `nodeIdFromSpatialId`（接入 spatial 八叉树）
- [x] SceneLoader 自动网格 AABB：`fillSceneBounds` 从 Assimp `aiMesh` 顶点计算网格节点 AABB，祖先节点取子节点并集（local 空间变换）；热重载保留
- [ ] 动画挂接 / 更完整脚本 Desc 值类型

代码：[`src/modules/scene/`](../../src/modules/scene/)

### 21. RPG 框架 — `rpg`

面向 RPG 类游戏的属性 / 效果 / 状态 / 技能 / 结算五套系统，全部以字符串为 key
数据驱动，核心可插拔扩展点（自定义属性运算、施法条件、结算阶段）留给 C++ 侧注册。
**设计文档：** [2026-08-08-rpg-module-design.md](RPG系统设计.md)。

- [x] 属性系统：任意字符串属性名 + 多重修改器（add/mulAdd/mulMul/override/clamp*/自定义 op）
- [x] 效果系统：数据驱动 `EffectDefinition`（C++ 注册 / JSON 加载）
- [x] 状态系统：施加/移除/到期、四种叠加策略（none/refresh/extend/stack）、周期 tick 事件
- [x] 技能系统：学习/冷却/消耗/读条/取消、自定义施法条件注册
- [x] 结算系统：具名可插拔流水线（优先级排序、取消、脚本可开关/重排）
- [x] `RPGActor` ECS 实体（三组件）+ 便捷成员方法 + `eve.RPG` 脚本绑定
- [ ] 与 `event` 模块的事件队列打通（目前 tick / cast 事件走独立的 push/poll 缓存）
- [ ] 存档序列化（属性/状态/技能运行时状态落盘格式）

代码：[`src/modules/rpg/`](../../src/modules/rpg/)
测试：[`test/rpg.cpp`](../../test/rpg.cpp)

### 21b. 背包 / 物品栏 — `inventory`

面向 RPG / 生存 / 经营类游戏的高扩展性背包框架：物品定义、固定格容器、跨包转移、
具名装备栏；接纳规则 / 容量策略 / 堆叠规则 / 变更钩子均可在 C++ 侧按字符串名注册。
**设计文档：** [背包系统设计.md](./背包系统设计.md)。

- [x] 物品定义：`ItemRegistry`（C++ 注册 / JSON 加载，tags/category/extra/equipSlot）
- [x] 容器 `Bag`：固定格、accept/reject tags、weight/volume、策略名字符串
- [x] 操作：add/remove/move/swap/split/merge/transfer（支持部分成功）
- [x] 可插拔：`registerAcceptRule` / `registerCapacityPolicy` / `registerStackRule` / `registerChangeHook`
- [x] 装备栏 `EquipmentSet`：具名槽 + 允许标签，与 `Bag` 穿脱；不硬依赖 `rpg`
- [x] `eve.Inventory` 脚本绑定 + 变更事件 poll
- [ ] 与 `ui` 的拖拽格子组件（由游戏或后续 UI 扩展提供）
- [ ] 存档序列化统一格式

脚本：`inv <- eve.Inventory()`  
代码：[`src/modules/inventory/`](../../src/modules/inventory/)  
测试：[`test/inventory.cpp`](../../test/inventory.cpp)

### 21c. 建筑放置 — `building`

面向策略 / RTS / 经营模拟（ETS）的高扩展性建筑放置框架：建筑定义、格子占用世界、
鬼影预览；校验规则 / 吸附规则 / 变更钩子均可在 C++ 侧按字符串名注册。
**设计文档：** [建筑放置系统设计.md](./建筑放置系统设计.md)。

- [x] 建筑定义：`BuildingRegistry`（C++ 注册 / JSON 加载，footprint/mask/tags/terrain/cost/extra）
- [x] 放置世界 `PlacementWorld`：占用图、地形语义、坐标换算、实例查询
- [x] 鬼影 `Ghost`：吸附、旋转、校验结果与失败原因
- [x] 操作：place / remove / move / placeGhost；cardinal 旋转占地
- [x] 可插拔：`registerValidateRule` / `registerSnapRule` / `registerChangeHook`
- [x] 内置：snap `grid`/`cell`/`free`；validate `default`/`boundsOnly`/`overlapOk`
- [x] `eve.Building` 脚本绑定 + 变更事件 poll
- [x] 示例 `examples/building`（城镇沙盒：地形/邻接/鬼影/旋转/拆除）
- [ ] 与 `ui`/`graphics` 的鬼影渲染组件（由游戏或后续扩展提供）
- [ ] 六角 / 等距格子拓扑（可用自定义 snap+validate 扩展）
- [ ] 存档序列化统一格式

脚本：`building <- eve.Building()`  
代码：[`src/modules/building/`](../../src/modules/building/)  
测试：[`test/building.cpp`](../../test/building.cpp)  
示例：[`examples/building/`](../../examples/building/)

### 22. Avatar 分层渲染 — `avatar`

人物 2D/3D 分层表现库：Image 图层叠加为基线，Live2D / VRoid 为可插拔后端。  
**设计文档：** [对话与Avatar模块设计.md](./对话与Avatar模块设计.md)。

- [x] Module + `AvatarInstance` 统一 API（string kind：`image` / `live2d` / `vroid`）
- [x] ImageAvatar：命名图层、zIndex、表情 spec、同步 `Renderable2D`
- [x] Live2D：`ILive2DBackend` + 内置 `null` + `registerLive2DBackend`（插件可换 Cubism）
- [x] VRoid：路径 / ModelData 绑定 + `Renderable3D` + Mesh morph 权重 bake
- [x] `avatar.update` / `sync` / `render(gfx)` / `bindTween`
- [x] Live2D 插件示例：`examples/live2d-backend-plugin`
- [ ] 正式 Cubism Core 绘制（用户自带 SDK）

脚本：`avatar <- eve.Avatar()`  
代码：[`src/modules/avatar/`](../src/modules/avatar/)  
测试：[`test/avatar.cpp`](../test/avatar.cpp)

### 23. 对话框及脚本 — `dialogue`

视觉小说风格对话舞台；**剧情继续写 Squirrel**（函数 / generator），不另发明 DSL。  
**设计文档：** [对话与Avatar模块设计.md](./对话与Avatar模块设计.md)。

- [x] 角色注册 / `bindAvatar` / 槽位 `left|center|right`
- [x] 台词打字机（UTF-8 码点）/ advance / narrate
- [x] 选项 present / select
- [x] `syncStage` 摆放 Avatar；表情与动作转发
- [x] 口型 `setLipSync*`（打字机包络 → avatar 参数 / Image 层 alpha）
- [x] 与 `animation` Tween 联动（`AvatarInstance::bindTween`）
- [x] 示例 `examples/dialogue`（generator + UI + lip-sync）
- [ ] 音频波形口型（可选，后续）

脚本：`dialogue <- eve.Dialogue()`（示例里常别名 `dlg`）  
代码：[`src/modules/dialogue/`](../src/modules/dialogue/)  
测试：[`test/dialogue.cpp`](../test/dialogue.cpp)


## 进度总览

| 模块 | 标识 | 状态 |
|------|------|------|
| 文件系统 | `filesystem` | [x] 基础可用 |
| 事件系统 | `event` | [ ] |
| 鼠标 | `mouse` | [x] |
| 键盘 | `keyboard` | [ ] |
| 触屏 | `touch` | [x] |
| 手柄 | `joystick` | [ ] |
| 窗口 | `window` | [x] 日常控制 API 已实现（呈现对接待 graphics） |
| 图形 | `graphics` | [ ] |
| 数据 | `data` | [x] |
| 图片 | `image` | [x] |
| 本地化 | `i18n` | [x] 可用（JSON 翻译表 / 占位符 / 复数 / 默认语言回退 / 热重载） |
| 声音 | `sound` / `audio` | [x] P0 可用 |
| 插件 | `plugins` | [ ] |
| 系统 | `system` | [x] 可用（OS/CPU/RAM/墙钟/休眠/剪贴板/电量/GPU） |
| 网络 | `network` | [ ] |
| 线程 | `thread` | [x] 可用（ThreadPool / Task / ThreadChannel） |
| GPU 计算 | `gpgpu` | [x] 可用（Compute shader + storage buffer；需 Vulkan 窗口已建） |
| 粒子 | `particles` | [x] 可用（ECS + 配置热更 + Camera/Canvas） |
| 物理 | `box2d` / `box3d` / `Physics` | [x] 可用（2D/3D 刚体 + Cloth + Fluid；示例 `examples/softbody`） |
| 逆运动学 | `ik` | [x] 可用（ik.hpp FABRIK；`eve.IK` / Skeleton2D·3D / Solver2D·3D） |
| 动画 | `animation` | [x] 补间 |
| 地图 | `map` | [x] 可用（Tiled JSON 子集 + 多投影 + 对象层 + 统一深度队列） |
| 程序化生成 | `procgen` | [x] Phase A 地图 + Phase B 贴图（[程序化生成模块设计.md](./程序化生成模块设计.md)） |
| 数据库 | `database` | [x] SQLite + CRUD/ORM + ECS 导出 + 脚本 API |
| 数学 | `math` | [x] 可用（Vec2/Vec3/Mat4 + noise + bezier + random） |
| 空间索引 | `spatial` | [x] QuadTree/Octree/Hash/BSP |
| 编辑器构件 | `editor` | [x] Gizmo / Brush / Toolbar·Inspector·Dock·History |
| 体素渲染 | `voxel` | [x] 32³ chunk + 贪婪矩形 + 实例化 + 可见性裁剪 |
| 张量 | `tensor` / `TF` | [x] TF2 风格（eager + `func` 图；`CompiledFunction` / 大张量 reduce 经 `gpgpu` 走 GPU，回退 CPU） |
| 实体 | `entity` | [x] ECS.hpp + 脚本 Entity/Component/System/view |
| 声明式 GUI | `ui` | [x] A+B（List/Component/Theme；脚本 Component 类待续） |
| 声明式场景 | `scene` | [x] 第一期（Host/Desc/Component/Transform；与 ui 同构） |
| 内置演示 | `demo` | [x] 可选（`EVENGINE_BUILD_DEMO`；程序化音效/行星贴图 + 默认 demo.nut） |
| RPG 框架 | `rpg` | [x] 首版（属性/效果/状态/技能/结算，全字符串 key，C++ 可插拔扩展点） |
| 背包 / 物品栏 | `inventory` | [x] 首版（物品/容器/装备/可插拔接纳·容量·堆叠规则） |
| 建筑放置 | `building` | [x] 首版（定义/占用世界/鬼影/可插拔校验·吸附规则） |
| Avatar 分层 | `avatar` | [x] 首版（Image 层 + Live2D/VRoid 接口） |
| 对话框及脚本 | `dialogue` | [x] 首版（Squirrel 剧情 + 打字机/选项/舞台） |
