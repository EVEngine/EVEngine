# Traversal reference parity

## 2026-09-13 unified air matching and contact continuity

- Ground, takeoff, fall and landing now render through the same schema-specific MotionMatcher. The auxiliary air player only tracks phase timing for the non-reference fallback; contact curves and generic inertia sample the pose source that is actually rendered. This removes the previous split where the Chooser state remained OnGround while a separate air clip was displayed.
- Movement-mode transitions retain the exact reference blend durations: jump 0.15 s, landing 0.20 s, ordinary ground/air changes 0.50 s. Same-schema landing reselection and cross-schema activation no longer replace an already queued movement-mode transition in the same fixed update.
- Mantle hand contact now preserves an authored right-hand plant when present. Pelvis reach limiting uses the posed shoulder-elbow-wrist chain length used by two-bone IK, avoiding the retargeted bind-length overestimate that allowed a 9.135 cm vertical fingertip displacement.
- `build/reference-selection-exact-mode-blends` passes the complete selection/render/contact suite, including explicit jump and landing duration assertions, local-pose handoff continuity, 12 fingertip contact cases, 24 consecutive traversals, Hurdle/Vault, moving support and high-platform falling. `build/reference-air-integrated` passes all 12 stand/walk/run/sprint and light/heavy air families. `build/climbing-motion-matching-unified` passes the complete input/collision/gravity/traversal regression.
- Render audit confirmed that the three grounded obstacle heights selecting the same `Mantle_1_0` family is source-consistent: the imported Chooser uses that family for OnGround obstacle heights 0-150 cm and reserves the low/medium/high catch families for InAir entries. No height-specific override was added.
- Generic BlendStack inertia now samples incoming matcher history in animation time using the active 0.85-1.15 play rate. At the first rendered frame after selection, current and previous target samples therefore bracket the actual selected instant instead of using an unscaled simulation-step offset. `build/reference-selection-playrate-inertia` passes the complete suite and its explicit 1.15x sample-time assertion.
- OffsetRootBone translation/yaw extraction, OrientationWarping root direction and contact/Phase curve derivatives now use the same play-rate-scaled animation step. Matcher curve switches inherit the external BlendStack duration (including 0.15 s jump and 0.20 s landing) instead of the intentionally disabled internal matcher blend time. `build/reference-selection-unified-timebase-2` passes the full suite and explicit external-curve-duration assertion.
- Steering root-angular-rate continuity now also uses the external BlendStack duration. The cached schema matcher's zero internal blend can no longer collapse angular velocity smoothing at a clip/schema switch. `build/reference-selection-unified-blend-owner` passes the full suite and asserts the jump root-rate window remains 0.15 s.
- `build/reference-selection-playrate-root-proof` adds real-runtime numeric coverage for root extraction across a loop boundary: a 1.15x matcher advances a 2 m/s source by 0.046 m over a 20 ms simulation step. The complete suite remains passing, proving the scaled sampler did not regress action selection, contacts or traversal exits.

Objective: fix all six identified differences against the local 游戏动画示例2 reference, in order. This is active work; a passing traversal smoke test is not completion of the objective.

1. Collision/display authority: never restore rejected capsule displacement into the rendered root. Keep explicitly authored floor/pelvis adaptation separate. Verify a second obstacle constrains both capsule and display, plus normal contact and release regression.
2. Conditional montage exit: import and evaluate Force Blend Out, With Movement Input and If Falling within notify windows; preserve duration and blend profile. Verify stationary completion, input arriving during a window, falling release, and cancellation. Version exported data; reject unsupported conditions without partial publication.
3. Continuous transitions: implement reference-relevant inertia, blend-stack interruption and bounded root offset/rotation behavior, with per-bone profiles. Verify position/velocity continuity across repeated switches and traversal release, not just endpoint poses.
4. Foot contact: source contact curves, planting/unplanting, ground tracing, pelvis compensation and leg IK during ordinary locomotion and traversal. Verify raised surfaces, edges, turns and moving support without foot sliding/penetration; explicit unsupported configurations.
5. Root-motion warp: implement source modifier translation/rotation semantics and moving target transforms, replacing ad hoc endpoint interpolation. Verify different heights, widths, approach angles, reachable contact and safe rejection when geometry cannot fit.
6. Schema-specific matching: implement the referenced schema channels/weights/horizons/normalization and traversal entry matching, replacing common-ground-only features and script phase filtering. Verify database and query encoders agree, schema routing, deterministic selection, representative rendered motion and performance.

## Evidence and current status

- Step 1 implementation changes `reference_root_motion.nut` to retain only pre-collision floor lowering; horizontal or vertical rejected collision displacement is never added back. Added real-engine blocker regression in `verify_reference_selection.py`; verified by `build/traversal-six-step1-final/result.json` (PASS): the second obstacle actually constrained motion while the displayed root could not restore it. The same run passed three contact heights, 24 repeated traversals, Hurdle/Vault landing, moving support, high-platform movement and camera checks. Engine-owned contact screenshot inspected.
- Step 2 fresh UE commandlet exports are in ignored `build/ue-reference/BP_NotifyState_MontageBlendOut{,_Default}.t3d`. Default condition is NewEnumerator0 (Force Blend Out), default duration 0.2 s, profile FastFeet_InstantRoot. Montage overrides include With Movement Input and 0.3 s. Received_NotifyTick calls Montage_StopWithBlendSettings and GetBlendProfileByName; do not replace these with an unconditional deadline.
- Step 2 data layer now emits selection schema 2 with `exits` records, resolves class defaults, preserves long source windows, and rejects unsupported conditions before publishing the montage. Runtime loader validates schema 2. All 34 montages regenerated; 8 converter tests pass. Runtime now evaluates notify conditions, releases native control at blend start and plays a separate advancing tail. Imported TimeFactor profiles apply per-bone durations; the actual root/feet early-completion check passed. `build/traversal-six-step2-profile/result.json` passes the full regression, with additional live EXIT_PROFILE_RUNTIME_PASS. Natural montage auto blend-out now resolves the exported AnimMontage CDO and per-asset overrides, uses remaining play time when trigger=-1, and uses the configured duration for an explicit trigger. Source Linear and HermiteCubic curves are honored. `build/traversal-six-step2-natural/result.json` passes, with natural-exit frame inspected; converter suite now 14 passing tests.
- Fresh `SK_UEFN_Mannequin.t3d` contains FastFeet_InstantRoot: TimeFactor, root explicit entry has omitted BlendScale (=0 per UE BlendProfile.h), legs and foot IK entries 0.75, unspecified bones default 1. Profiles are now compiled from actual entries and applied to live tail playback. Converter suite: 10 tests passed.
- AC_TraversalLogic EventGraph confirms OnBlendOut clears DoingTraversalAction, restores IgnoreComponentWhenMoving(false), then SetMovementMode_CMC. OnCompleted is unlinked. Therefore runtime control is handed back at blend start while the visual montage tail blends out, not after waiting out the fade.
- Historical checkpoint: steps 3–6 were pending here; later entries below record subsequent implementation and remaining limitations.

Native public API, persistent data and lifecycle changes must follow the four architecture specifications in AGENTS.md. Preserve a single motion authority, checked Result contracts, injected fixed time, generation-checked support handles, versioned atomic import and source-only architecture gate plus fixtures. Do not claim completion from existing tests that do not cover these requirements.

- UE local source `Engine/Source/Runtime/Engine/Private/Animation/AnimMontage.cpp` constructor defaults: BlendOut 0.25 s, BlendOutTriggerTime -1, bEnableAutoBlendOut true. Stationary montage should naturally begin blending before sequence end, not use the generic frozen transition at the last frame. Export AnimMontage CDO to make default resolution data-driven; preserve any per-asset overrides. Implemented and verified. The fresh CDO actually specifies HermiteCubic, unlike the earlier assumption of Linear; the notify Blueprint explicitly uses HermiteCubic too. The 34 assets all enable auto blend-out; unsupported indefinite-hold assets are rejected instead of silently auto-ending.

- Next: step 3. Existing ControlPose is a continuously tracking second-order/spring/PD driver with quaternion-component normalization; do not mistake it for finite-duration pose inertialization or duplicate its continuous-control role. MotionMatcher still snapshots fromPose_ and blends it with matchedPose_. Existing FootIKSolver has contact smoothing, planting and pelvis compensation and should be evaluated for step 4 reuse. Final step-2 architecture gate and its 9 fixtures were launched in sessions 12421 and 67740; inspect their logs before starting a duplicate run.

- Step 3 native matcher now uses an owned finite-duration pose offset instead of frozen-source crossfade. Translation/scale offsets and quaternion log-space rotation offsets decay with a quintic curve; prior actual output and incoming clip history seed offset velocity, including interrupted transitions. Incoming clips continue sampling. This is an internal animation implementation, with no new public operation, persistent schema, cross-module relationship, or discarded Result. All MotionMatcher header consumers rebuilt in the build log. `build/traversal-inertia-tests.log`: 18 matching tests pass, including three new inertia/real matcher integration cases. Step-2 fixture process finished: 9/9 PASS. Current architecture source gate passes (`build/traversal-inertia-architecture.log`). Real scene regression is running in `build/traversal-inertia-runtime`; do not yet claim step 3 complete. Script action handoffs still use frozen transitionPose; per-bone inertia and bounded root translation/rotation policies remain pending, along with steps 4-6.

- Step 3 matcher runtime regression completed: `build/traversal-inertia-runtime/result.json` PASS, including 24 consecutive traversals, conditional/natural montage exits, high-platform falling, moving support and rejected-displacement authority. Engine-owned Dense-run-left and high-platform-falling frames inspected; these frames verify rendering/representative pose, not full temporal coordination. Module dependency gate passes. The interactive engine is left running by the verifier. Next implement inertia for script action/air handoffs and reference bounded root policies; current matcher change alone does not complete step 3.

- Step 3 handoff implementation: added native `AnimInertializer` with copied pose history, atomic checked begin/evaluate, quaternion-log inertia and per-bone time factors. The example's `pose_transition.nut` queues actual outgoing render history for action/air/bank changes and samples incoming clip history at the same instant. All old `transitionPose` frozen crossfade call sites now queue inertia; root factor zero preserves collision authority. Authored montage exit tails remain separately advancing. Public API uses Pimpl and owns inputs, explicit simulation elapsed time, no callbacks, no cross-domain handles/ownership or persistence changes. Player bindings were moved out of the >1000-line Animation.cpp before adding borrowed getClip access. Native test suite: 21 matching/inertializer cases pass, including atomic rejection, factors, replay and interrupted rotation/velocity. Strict binding, test discovery and module dependency gates pass. Source architecture gate passes; fixture suite and real-engine regression are in progress in `build/traversal-handoff-*`. New runtime regression asserts actual rendered pose continuity through air/landing interruption. Remaining step-3 work includes authored entry transition policy and bounded root offset/rotation behavior; do not mark all six complete.

- Handoff runtime result is now PASS (`build/traversal-handoff-runtime/result.json`), including the new rendered-pose interruption check and all 24 repeated traversals. Architecture fixtures completed 9/9 in 122.996 s. Inspected engine-owned traversal-1-contact and conditional-mantle-exit frames: the exit is rendered correctly, but the low mantle still visibly penetrates the platform with the lower leg. This is unresolved step-4/5 contact/path geometry, not evidence that all visual issues are fixed. Current live example PID is recorded in the runtime output folder (29312 at completion); verify path before stopping. Continue step 3 authored entry policy/bounded root rotation and translation next, then actual foot-contact and warp geometry corrections.

- Step 3 authored entry now follows freshly inspected AnimMontage_Default: BlendModeIn Standard, BlendTime .25, HermiteCubic, no profile. Of 34 source montages, 33 inherit this curve and one overrides Linear. Compiler selection schema 3 imports entry mode/duration/curve/profile, rejects unsupported modes/curves before publishing, and includes entry-only profiles. Runtime requires schema 3; older generated assets must be regenerated. All 34 private montage records regenerated; 17 converter tests pass.
- Standard entry is distinct from generic inertia: outgoing source animation continues sampling and its existing blended residual/velocity is preserved with native inertia; it is then blended into the moving incoming animation with the authored curve and per-bone time factors. Root bone remains target-owned. Reset/exit explicitly cancel long-duration transitions rather than relying on time=1. `build/traversal-entry-runtime/result.json` passes the full regression (24 repeated traversals, platforms, exits, collision authority). Added ENTRY_BLEND_PROBE to the real verifier and executed it against the same live engine (`build/traversal-entry-probe.log`, runtime STANDARD_ENTRY_RUNTIME_PASS), proving outgoing playback advances and both curves and .75-foot profile match expected motion. Remaining step 3: bounded root offset/rotation policies; steps 4-6 remain incomplete. Entry source residual preserves interruption continuity but is not a literal unbounded UE blend-stack graph.

- Next root policy evidence (freshly inspected local exports and UE 5.8 source): `build/root-policy-inspection.log` plus `build/reference-Get_OffsetRoot{TranslationHalfLife,RotationMode,TranslationMode}.json`. Translation mode is Release for DefaultSlot montage, air or stopped; Interpolate while grounded moving. Half-life function overrides the node default .2: stopped .1, moving .3 seconds. Rotation mode Release with DefaultSlot active, otherwise Accumulate; rotation half-life .1, max translation error .30 m. UE `AnimNode_OffsetRootBone.h` defaults max rotation error -1 (unbounded), clamp-to-velocity flags false and collision testing Disabled; do not invent a source angular clamp. Runtime source at Engine/Plugins/Animation/AnimationWarping/Source/Runtime/Private/BoneControllers/AnimNode_OffsetRootBone.cpp: Accumulate/Interpolate consume root delta and do not counter capsule delta; Release counters capsule motion and dampens residual. Ground projection precedes damping and max-distance clamp. Existing collision authority must remain enforced when implementing this adaptation. Current live entry-verification engine PID 36508 (verify path before stopping). Entry rendered frame inspected; unresolved low-ledge body/leg geometry remains step 4/5.

- Entry change final gates: architecture source contract PASS, fixtures 9/9 (194.281 s), strict bindings and module dependency gates PASS. Converter tests 17/17 and live Standard-entry probe PASS. No deliberate architecture exception. Root policy implementation is still next, not completed by the source inspection above.

- Step 3 translation offset implemented in `reference_root_offset.nut`, composing existing native clip root extraction and read-only capsule shape casts with fixed-time planar residual state. Grounded moving uses Interpolate (.3 s), idle/air/slot uses Release (.1 s when idle), max .30 m. Cast queries leave mover state untouched and reject unsupported visual positions at ground edges. Pure damping and live integration probes verify half-life, timestep partition, distance cap, actual accumulated movement, idle release and root-loop boundary extraction. Full `build/traversal-root-offset-runtime/result.json` PASS (24 traversals, exits, moving/high platforms, collision authority), expanded probe passed live (`build/traversal-root-offset-probe.log`). Root rotation remains deliberately unimplemented until Steering, because source Accumulate without Steering would prevent ordinary directional clips from following target heading. No public API/persistence/module boundary changed this step.

- Translation-offset source architecture gate PASS; inspected engine-owned stop/high-platform-falling frames. Six-second real runtime profile (`build/traversal-root-offset-runtime/performance.txt`): 60.19 FPS, fixedUpdate 1.921 ms, root offset update 0.193 ms average (362 calls). Live engine PID 14116 at validation; verify path before stopping. No native/public API or persistent-format edit this step; prior entry change architecture fixtures remain 9/9 and unchanged. Rotation/Steering must be implemented next; translation does not complete step 3 or the six-item goal.

- Step 3 rotation/Steering regression is now PASS in `build/traversal-steering-complete/result.json`: authored yaw consumption, predictive steering, Accumulate/Release, and angular-rate continuity at matcher switches. Includes 24 repeated traversals, exit/contact authority checks and left-direction assertion. Actual TurnInPlace graph compares OrientationIntent with RootTransform, not the capsule heading; implementation follows the linked pins. This remains a bounded approximation to the complete UE per-clip Blend Stack, not full graph equivalence. The latest low-mantle contact frame still visibly penetrates the obstacle with a lower leg; step 4/5 remain necessary.

- Step 4 source data work: added read-only `ue_export_contact_curves.py`, verified against the actual reference project via UE commandlet (exit 0). `build/ue-reference/contact-samples.json` contains 2,008 sequence records: 1,342 paired contact curves, 666 explicitly absent. It is a 78,232,177-byte intermediate evaluation capture at 240 Hz plus authored key times, **not a runtime payload**. Compact encoding and sampling validation must precede runtime integration. Comparing UE evaluation with linear interpolation of previously archived keys found maximum contact-weight error 0.16126 (Relaxed Walk Turn 180 R), proving old key-only interpolation changes contact timing. Audit: `build/ue-reference/contact-samples-audit.json`.
- New extractor contract test passes: source evaluation is retained (including off-grid key time), absence is explicit, changed source and nonfinite curve values preserve the previously published artifact. Schema `eve.unreal-contact-samples/1` is versioned, with source hash checks and atomic replacement; sampled interpolation is documented as approximate between samples. No source UE packages are saved.
- Step 4 implementation constraint: existing FootIKSolver stores lock contacts in model space despite the world-lock method description, and releases only by contact weight. Do not simply enable it for parity: world/support-relative anchoring, distance/angle unplant/replant, blended contact signals and ground/ball orientation need explicit handling. Source FootPlacement also has a linked Get_FootPlacementPlantSettings input, so inspect its dynamic overrides before treating serialized node fields as final settings. Runtime Foot Placement is not yet implemented.
- Step 4 compact curve compiler now produces `eve.animation-contact-curves/1` (6,230,199 bytes, 2,008 records) from the UE evaluation capture. Verified all 3,103,981 playable samples against compressed linear segments: maximum error 0.000989151, below the 0.001 contract (`build/ue-reference/contact-reduction-audit.json`). UE source curves often include pre/post-roll keys; these are validated, with runtime curves trimmed at explicitly evaluated sequence endpoints rather than lengthening the animation. Two focused tests cover sample fidelity, off-grid keys, playback extent, explicit absence, unknown fields, source changes and atomic failure. No runtime contact consumer has been connected yet.
- Contact extraction/reduction source architecture gate PASS; architecture fixtures 9/9 PASS (183.655 s), diff whitespace check PASS. Persistent intermediate format/atomic publication rules apply; no native public API, cross-module lifecycle, or ECS change and no deliberate exception. Fresh `build/reference-Get_FootPlacementPlantSettings.json` shows dynamic choice between PlantSettings_Default and PlantSettings_Stops, driven by Stops database tag (or Stop blend-stack tag for experimental state machine). Export actual Blueprint class defaults next; do not substitute static FootPlacement node values. Then connect compact curves to blended animation playback and support-relative foot placement.
- Actual Blueprint CDO read succeeded through UE (`build/foot-defaults.log`). Default/Stops both use SpeedThreshold=1 cm/s on remapped `(1-contact)*100`, DistanceToGround=10 cm, PivotAroundBall, UnplantAngle=60 degrees, ReplantAngleRatio=.2, UnalignmentSpeedThreshold=100. Default UnplantRadius=20 cm / ReplantRadiusRatio=.2; Stops radius=40 cm / ratio=.75. MaxExtensionRatio=.5, MinExtensionRatio=.2, AnkleTwistReduction=.75, reconstruct-velocity and adjust-heel-before-planting false. The serialized node defaults do not override this connected input.
- Contact channel runtime integration now loads source-addressed schema `eve.animation-contact-runtime/1`, checks all 1,879 reference clips, samples active player or matcher, advances Standard entry/exit curves and preserves scalar value/velocity through generic switches. Looping, missing channels and held terminal time have focused real-engine assertions in CONTACT_CURVE_PROBE. A first probe exposed wrapper identity differences from AnimPlayer.getClip; mapping now uses explicitly unique clip names, with duplicate rejection. Three Python extractor/reducer/binding tests pass. Full real-engine verification is `build/traversal-contact-bound` (in progress at this entry). World/support-relative planting and leg/pelvis solving are still not connected.
- Performance limitation to resolve before final delivery: the 6.2 MB Squirrel curve table adds substantial traced startup cost (first-present 32.690 s in `build/traversal-contact-probe/runtime.log`). Replace it with compact native loading rather than claiming the script data path meets the user's startup requirement. Do not disable tracing globally as a workaround. The intermediate representation is private generated data and must not become a second authoritative curve source.
- Contact binding verification completed: `build/traversal-contact-bound/result.json` PASS; runtime log contains REFERENCE_CONTACT_CURVE_RUNTIME_PASS for looping, missing channel, live player binding, continuous handoff, settling and held terminal sample. All 24 traversals and platform/exit regressions also pass; engine-owned walk frame inspected. Six-second real profile: 60.2056 FPS, contact channel update 0.046 ms average, fixedUpdate 2.037 ms (362 calls). Startup remains 32.852 s and must be repaired with native data loading before calling the work complete. Architecture source gate and diff whitespace check PASS. Next implement native compact curve loading, then source-derived support-relative Foot Placement; current channel output alone does not fix limb penetration.
- Contact binding architecture fixtures completed with exit 0 (9 tests); `build/traversal-contact-binding-fixtures.log`. Persistent runtime schema has checked source binding and atomic compiler publication; no native public API or cross-domain ownership change and no deliberate architecture exception.
- Native curve loading replaces the script table: owning Pimpl `AnimCurveLibrary`, checked atomic load and optional-valued sampling, strict EVFC/1 format (`eve.animation-curves/1`). It retains no clip/skeleton/filesystem pointers and takes explicit sample time. Binding reads filesystem bytes only during synchronous load. Compiler verifies source hashes/coverage, emits Float32 keys and rejects time collisions; runtime preserves absence and uniquely maps player clip names to stable source paths. Old private `contact-curves.nut` removed; README documents migration and required producer commands.
- Native verification: 2 CTest cases pass, including positive/negative/large-time wrap, clamp, absent-vs-unknown source, every truncation, invalid versions/UTF-8/duration/key order/endpoints, source/channel duplicates and resource counts, with prior data preserved on all rejections. Four Python tests pass. Binary artifact is 1,632,305 bytes; all 3,103,981 playable source samples checked against packed interpolation with maximum error 0.000989151 (`build/ue-reference/contact-binary-audit.json`). Source architecture, strict bindings, module dependencies, test discovery, diff checks PASS; architecture fixtures 9/9 (116.943 s). Public Result/ownership/time/persistence requirements apply with no deliberate exception.
- `build/traversal-contact-native-final/result.json` PASS, including native contact loop/absence/player-handoff/held-time probe and all 24 traversal/platform/exit regressions. First failed native run exposed unsupported `double` Squirrel argument conversion; adapter now takes script Float32 and passes it to native double-time evaluation. Successful run: contact load .285 s; total first-present 17.184 s; reference selection initialization 7.370 s including .999 s database bake. This is not proof total startup is resolved; earlier attribution of all startup delay to the curve script was too broad. Six-second real profile: 60.1962 FPS, native curve script adapter .127 ms/frame, fixedUpdate 2.130 ms (362 calls). Next source-relative planting/leg/pelvis integration remains required; low-mantle penetration is still unresolved.

- Step 4 now connects blended contact curves to ball-pivot planting, source Default/Stops distance and angle hysteresis, ground queries, support-relative anchors, pelvis lowering and analytic leg IK. Fresh UE CDO interpolation settings override the static node: release stiffness 100 by default, 250 for Stops. Airborne traversal returns toward authored feet/pelvis instead of pulling the body toward the floor beneath it. Transition history is captured before foot processing, preventing double IK on interruption; zero-time pose evaluation does not advance spring history.
- `build/traversal-foot-support-link/result.json` PASS: full real-engine regression including 24 traversals, high-platform support/exit, collision authority, actual rendered handoff continuity, planted ball stability and moving-support anchors. Support links combine world identity with a fresh ray query ID, retain no Body3D pointer, and clear before world replacement. World3D assigns monotonically increasing query IDs. Probes cover body destruction, foot-link destruction and world mismatch; this is a course-local observed link, not a general physics handle API. Fixed time is injected; no collision displacement is restored by IK.
- Analytic two-bone solving replaces the old two-pass CCD implementation and removes the example's 24 repeated solves. `build/foot-ik-regression.log`: 8 tests PASS, including reachable target precision, unreachable clamping, zero weight, degenerate limbs and a rotated uniformly scaled parent. Architecture source gate PASS (`build/foot-placement-final-architecture.log`); architecture fixtures 9/9 PASS (`build/foot-placement-fixtures.log`). Existing public signature retained; ownership/time/Result contracts for the surrounding additions remain required, with no deliberate exception.
- Remaining step 4 gaps: floor-normal/ankle alignment, floor/angular interpolation and the complete source pelvis extension/compression/rebalance solve. Current pelvis solve is downward-only. Low-mantle lower-leg penetration remains visible in inspected engine-owned contact frames. Step 5 still uses endpoint correction and vertical-only moving-target following, not source SkewWarp translation/rotation semantics. Step 6 still routes custom schemas through common default features. Passing current probes does not establish full graph parity or resolve these visible defects.
- Follow-up step 4 replaces downward-only pelvis selection with source-derived desired/max/min limb ranges, heel-lift allowance, horizontal reach limit, .3 horizontal rebalance and combined two-leg height selection. The source node overrides MaxOffset=250 cm and stiffness=100; inherited HeelLiftRatio=.5 and MaxOffsetHorizontal=10 cm are preserved. Compression uses the authored IK-root plane distance (minimum ankle/ball), not physical floor clearance. Metre-space bias is .0005 m, corresponding to source .05 cm. Explicit fixed-time spring state resets with the course. No new public API, persistence or cross-domain owner is introduced.
- `build/traversal-pelvis-source-plane/result.json` PASS after the plane correction, including synthetic raised/lowered-support range checks and the full real-engine traversal/platform/handoff suite. Engine-owned low-mantle contact frame inspected: lower-leg penetration remains. Remaining foot work includes full ground/ankle alignment, interpolated plane, and post-pelvis heel/extension correction; this range solve does not complete step 4. The current vertical sphere intersection assumes the ordinary hip-above-foot arrangement; extreme above-hip targets still need explicit source parity coverage.
- Above-hip intersection handling now follows FMath::SphereDistToLine's nearest-to-line-origin choice (TraceSettings.EndOffset=100 cm), including tangent/no-intersection height and equal-distance downward selection. The earlier always-upper intersection was wrong for targets above the query origin. New above-hip and tangent real-script checks pass in `build/traversal-pelvis-above-hip/result.json`.
- Ground alignment no longer forces the ankle to a fixed .08 m above the floor: it preserves the authored ankle-to-IK-plane vertical distance. Plant eligibility uses the minimum authored ankle/ball plane distance, matching CalcTargetPlantPlaneDistance. `build/traversal-authored-ankle/result.json` PASS with horizontal/tilted plane-distance probes and full traversal/platform/lock/handoff regression; engine-owned walk frame inspected. Source architecture gate PASS (`build/foot-authored-height-architecture.log`). Changes are private example algorithms with explicit fixed time and existing owners; no public API/schema change or deliberate architecture exception.
- Further source comparison: UpdateLegInputPose uses IKIndex as the placement input and reconstructs the ball relative to that IK transform, then LegIK drives FK. The example still starts from FK foot/ball. Measure actual imported IK/FK deviation and transfer this target path before claiming equivalent FootPlacement; do not assume both tracks coincide. Ground normal/rotation/plane interpolation and post-pelvis heel correction also remain pending, as do steps 5 and 6.
- IK/FK audit completed in the live engine: 15,032 samples (four times per compatible clip, both feet), maximum positional difference .0911492 m in M_ragdoll_poses_rolling_0 at normalized time .25, left foot (`build/traversal-authored-ankle/runtime.log`, IK_FK_AUDIT). This corpus maximum is not a claim that ordinary walking differs by that amount. FootPlacement now uses ik_foot_l/r as input, reconstructs the ball using the FK foot-to-ball relative rotation/translation, and drives the FK leg with the resulting target. Physical limb lengths remain measured from the FK chain.
- `build/traversal-ik-target/result.json` PASS, including rotated IK/FK ball mapping, planted ball stability (120 samples, maximum error 4.95338e-7 m), rendered handoff continuity and the full 24-traversal/platform suite. Engine-owned moving-platform contact frame inspected. Architecture source gate PASS (`build/foot-ik-target-architecture.log`). No public API, persistence or ownership change; no deliberate exception. Source ground-plane orientation/interpolation and post-pelvis correction remain next; the six-item objective is still incomplete.
- Step 4 ground alignment now retains ray-hit point/normal, projects ankle height along the approach direction, and transfers source IK-plane orientation to the actual plant plane. New private `reference_foot_alignment.nut` implements the source plane delta, swing/twist separation, and .75 ankle twist reduction while retaining authored pitch on flat terrain. Alignment is weighted by contact-derived alignment alpha. No native/public API or lifetime/schema change.
- `build/traversal-foot-normal/result.json` PASS: full traversal/platform/handoff regression plus pitch, roll reduction and flat-pose preservation probes. Expanded FOOT_PLACEMENT_PROBE also ran against the same live engine after completion (`build/foot-normal-live-probe.json`, ok), checking a real 20-degree rotated physics box's returned normal. Inspected engine-owned stop frame; this is flat-terrain visual evidence, not a sloped walking sequence. Source architecture gate PASS (`build/foot-normal-architecture.log`), diff check PASS. No deliberate exception. Plant-plane interpolation, temporal angular release and post-pelvis heel/extension correction still need implementation and sloped moving-character visual validation. Steps 5/6 remain incomplete.
- Grounded plant-plane smoothing now preserves the previous plane and last aligned foot point, chooses the closer prior vertical intersection, consumes component vertical displacement only to close the gap, and uses source floor stiffness 1000 / angular stiffness 450 with critical damping. Height lag is bounded by the node's .20 m penetration allowance; this is interpolation policy, not a capsule collision override. Normal rotation uses a model-independent world rotation-vector spring. Per-foot state is owned/reset with the existing placement state; explicit dt, no public API or persistence change.
- `build/traversal-floor-smoothing/result.json` PASS, including discontinuous height/normal changes, zero-time preservation, settling, real slope ray, and the full traversal/platform/handoff suite. Engine-owned high-platform-falling frame inspected. Source architecture gate completed successfully (`build/floor-smoothing-architecture.log`). Remaining source differences include interpolating toward the virtual IK plane during air, angular unplant release, post-pelvis heel/extension correction and moving-character slope visual evidence. This grounded interpolation step alone does not establish full FootPlacement parity; six-item objective remains active.
- Air now routes foot alignment through the same persistent plane spring, targeting the authored IK-root position with world-up normal. Actual physics support stays separate: the virtual plane cannot plant a foot or retain a support link. Air interpolation does not consume capsule vertical movement or apply the real-ground penetration clamp. Zero-weight feet still update their last evaluated foot point for subsequent plane interpolation. The pelvis continues returning to zero offset in air; complete source post-pelvis correction remains pending.
- `build/traversal-air-plane/result.json` PASS, including air plane lag/settling, no ground-only compensation on a one-metre rise, all prior foot/plane probes and the full traversal/platform/exit regression. Engine-owned high-platform hand-contact frame inspected; hand support remains visually present. Architecture source gate PASS (`build/air-plane-architecture.log`), no public API/schema/lifecycle change or deliberate exception. Remaining step 4: angular release, heel/extension/compression finalization, rotating/sloped support visual verification. Source performs a trace even in air before replacing the plane; current adapter intentionally queries physical support only while grounded, so this is still a documented parity difference requiring review, not full equivalence. Steps 5 and 6 remain incomplete.
- Source-derived post-pelvis finalization is now connected before analytic leg IK: calculate maximum extension from the authored pose, gate heel lift on planted/reachable or recently unplanted state, pull by at most the foot length times alignment alpha, rotate toward the preserved toe direction, clamp remaining extension, correct plant-plane penetration with authored penetration allowance, then enforce minimum vertical compression. Reachability and time-since-unalignment are per-foot resettable state. The source heel-roll approximation itself does not exactly preserve toe position after translation; this adapter follows that calculation rather than claiming an exact toe pivot.
- `build/traversal-foot-finalize/result.json` PASS, including independent heel lift/reach and above-hip compression probes, locked ball stability, actual rendered transition continuity, 24 traversals and platforms. Source architecture gate PASS (`build/foot-finalize-architecture.log`), diff check PASS. No public API/persistence/owner change and no deliberate exception. Inspected low-mantle contact frame still shows lower-leg penetration; finalization does not repair the root path. Remaining step 4 work includes angular unplant release and sloped/rotating support visual coverage; step 5 SkewWarp and step 6 schema matching remain necessary.
- Angular-release source audit resolves an earlier overbroad pending item: GetFootPivotAroundBallWS takes the current ball rotation and reconstructs the foot from the current BallToFoot transform. For this source lock mode, the unaligned rotation equals the current input rotation, so the stored rotational offset is identity; adding extra ankle damping would alter authored motion. This does not implement the unused LockRotation mode, which is not selected by either imported plant policy.
- Fixed a real release discrepancy: previously the whole world-space ankle target was spring-smoothed. Source ProcessFootAlignment resets velocity while planted, stores the planar root-relative lock offset, and springs that offset to identity on release. Runtime now follows this structure; authored swing advances immediately while the residual offset decays, and planted vertical alignment preserves the animated ankle height. Reset owns/clears the new offset state, with explicit dt and no API/schema/lifetime change.
- `build/traversal-unplant-offset/result.json` PASS for the complete runtime suite. Expanded FOOT_PLACEMENT_PROBE ran on the completed live engine (`build/unplant-offset-live-probe.json`, ok): with zero lock offset and an artificially stale target .25 m away, the target follows the current authored foot immediately. Engine-owned walk frame inspected. Moving/sloped support visual coverage and remaining trace/curve fidelity must still be audited before step 4 is complete; low-mantle penetration and steps 5/6 remain unresolved.
- Added repeatable live `scripts/verify_reference_sloped_support.py` (existing MCP port, output directory); it saves/restores update/camera hooks and platform rotation even on failure, captures standing/rotating/walking frames and records responses. Real slope testing found an actual controller defect: raw centre-ray floor snapping embeds the capsule sphere in slopes, and the constant grounded -.5 m/s downward sweep projects into downhill sliding. First rotating test lost the platform (151 grounded frames, only 211 foot support samples, final z=12.7396). Correcting sphere support height alone still drifted .1003 m on an idle 10-degree slope.
- Controller now uses the tangent support height hitY + radius*(1/normalY-1), rejects non-walkable normals, and checks distance to that adjusted height. Grounded motion no longer injects a downward sweep; existing final support probing handles adhesion/edge release. Capsule position remains authoritative. No native/public API, schema or owner change and no deliberate exception.
- `build/traversal-slope-stable/result.json` PASS, preserving full traversal/platform/edge/falling regressions. `build/sloped-stable-proof/responses.json` and runtime SLOPE_* markers prove: 180/180 static support frames with idle drift under .05 m, 180/180 rotating support frames, 720 foot samples with minimum bone-to-plane clearance .0271699 m, and .616666 m sloped walking displacement. Engine-owned close walking frame inspected. These tests cover 10-degree standing and slowly varying approximately 5–15-degree pitch, not arbitrary platform angular speeds. Source architecture gate PASS (`build/slope-stable-architecture.log`), diff check PASS. Low-mantle penetration remains a root-warp issue; source trace/curve edge cases and steps 5/6 are still pending.
- Step 5 source audit: 72 SkewWarp windows across exported montages; 21 disable rotation, 11 override rotation time multiplier to .8, all override IgnoreZAxis=false, 70 override added-translation easing to Linear. Two residual `interaction` bone names have provider None and must not enable bone targeting. The existing compiler drops these flags and unconditionally consumes nonempty bone names, so runtime endpoint correction cannot be considered source-equivalent.
- Exporter now includes RootMotionModifier_SkewWarp class defaults. Fresh UE commandlet exit 0 (`build/skew-default-export.log`) produced `build/ue-reference/RootMotionModifier_SkewWarp_Default.t3d`: default provider None, translation/rotation true, IgnoreZAxis true, feet-location true, easing HermiteCubic, Default/Slerp rotation, time multiplier1, speed/rate clamps0. CDO delegate fields contain unsupported `(null).None` syntax: read only needed configuration fields, as for AnimMontage defaults, rather than broadening unrelated parsing.
- Added pure `unreal_warp_settings.py` configuration resolver; three focused tests pass for inherited defaults, overrides, unused bone names, invalid/unsupported policy rejection and input preservation. All 72 actual windows resolve successfully (`build/ue-reference/warp-settings-audit.json`). This resolver is staged and is not yet wired into selection serialization or runtime; no new persistent schema or runtime behavior is claimed. Next integrate a versioned window contract and the actual SkewWarp step, including translation-free windows, rotation and dynamic targets. Step 4 trace/curve boundary parity remains to audit; do not mark the overall goal complete.
- Step 5 configuration resolver is now wired into montage compilation. Schema `eve.animation-selection/4` flattens complete window configuration into each window record, hashes the modifier CDO source, resolves the explicitly referenced modifier instead of taking the first nested object, rejects invalid windows before publishing a montage, and clears unused bone names when provider=None. Runtime checks window fields/flags/provider/policy and rejects schema3; regenerate private selection files. All 34 montages/72 windows regenerated, retaining 21 disabled-rotation windows and 11 .8 time multipliers.
- 21 converter tests PASS, including selected-child resolution, inherited easing, unused bone names and no partial publication for malformed time windows. `build/traversal-warp-schema4/result.json` PASS for full real-engine regression after target-provider correction; low-mantle contact frame inspected and still penetrates. Configuration is now available/validated, but the old root solver still does not consume the full SkewWarp policy; this remains explicitly incomplete. Source contract gate PASS (`build/warp-schema4-architecture.log`); fixture suite is tracked in `build/warp-schema4-fixtures.log`. Persistent format rules apply: explicit version, rejected unknown window fields, atomic runtime .nut replacement and source hashes; no deliberate exception.
- Schema4 architecture fixtures completed: 9/9 PASS in 157.634 s (`build/warp-schema4-fixtures.log`). Next replace root endpoint interpolation with source animated/no-translation branches and actual rotation/target transform processing; neither schema migration nor these tests completes that implementation.
- Step 5 window translation now integrates authored deltas against remaining source displacement and remaining target displacement. The scale/shear is expressed as longitudinal redirection plus preserved transverse delta, with the source perpendicular-target branch. Frames split at window boundaries; same-time queries return cached results and backward simulation time is rejected. Translation-free windows use their imported Linear/HermiteCubic easing. Dynamic target height is evaluated during the window. Removed obsolete from/to smooth-correction state; capsule collision still owns applied motion.
- `build/traversal-skew-translation/result.json` PASS for full runtime regression. Expanded live probe passed (`build/skew-translation-live-probe.json`): transverse-motion preservation, endpoint reach and perpendicular target branch. Source contract gate PASS (`build/skew-translation-architecture.log`), unchanged architecture fixtures remain schema4 9/9. No native/public API or persistent schema change; explicit monotonic simulation time and private traversal-owned state. Low-mantle contact frame inspected and still penetrates. Remaining: full bone-to-root target transform, root/target rotation, full moving target transforms, speed caps/feet-location variants, feedback from collision-resolved position, and removing the legacy post-contact Mantle endWarp correction. Current passing tests do not cover these pending semantics. No deliberate architecture exception.
- Direct low-mantle diagnosis changed the next action: root and attach sampled rotations are both identity at the contact pose, so rotation is not the cause of this particular penetration. Actual FrontLedge target was z=4.97, .33 m inside the physical front face z=4.64; hanging calf was z=4.80226 below the .62 m top. Native candidate.topPoint is an inset support ray, not the edge. Source SetWarpTargets uses FrontLedgeLocation plus .5 cm vertical clearance. Reference geometry now combines candidate.frontPoint horizontal coordinates with confirmed top height + .005 m. Top inset remains separately available for native landing-distance calculations.
- `build/traversal-front-edge/result.json` PASS for full runtime regression after correcting FrontLedge. The engine-owned low-mantle contact frame now visibly keeps the hanging lower leg outside the platform. Added a below-top knee/front-plane assertion to the existing contact checks; the expanded three-height probe passed live (24 knee/hand samples, `build/front-edge-knee-probe.json`, FRONT_EDGE_KNEE_PASS). This resolves the repeatedly observed low-mantle contact penetration, not every possible traversal pose/geometry. Source architecture gate PASS (`build/front-edge-architecture.log`), no public API/schema/ownership change or deliberate exception. Full target transform/rotation, dynamic targets, post-contact warp and schema matching remain required.
- Step 5 collision feedback: UE SkewWarp reads OwnerAdapter current visual root for every extraction. Runtime now separates the cached requested point from the next extraction origin; after native collision resolution, the origin uses actual capsule feet plus only the explicit pre-collision floor lowering. Same-time diagnostic reads remain stable. Full `build/traversal-warp-feedback/result.json` PASS, including the expanded live blocker assertion that the next extraction origin equals the resolved visual root, 24 repeated traversals, three-height contact checks, moving support and falling. Source architecture gate and diff whitespace check PASS. Private state only, explicit simulation time, no API/schema/ownership change or deliberate exception. Legacy post-contact endWarp still overrides the integrated path and must be removed as part of the remaining full target-transform/rotation work; this correction completes feedback for the integrated windows, not all six requirements.
- Step 5 target/rotation integration: source `CalculateRootTransformRelativeToWarpPointAtTime` and `WarpRotation` inspected directly. Bone providers now compose inverse bone transform with root transform and the world ledge transform; provider None targets root directly. Front-face normal determines target yaw. Each extraction rotates source translation through inverse source root orientation and current traversal orientation, consumes authored quaternion delta, and applies Default/Slerp remaining-error correction with imported rotation flag/time multiplier. Full quaternion state drives visual root residual while capsule heading consumes yaw. No new public API, persistent format or lifecycle owner; explicit step time and no deliberate architecture exception.
- `build/traversal-warp-rotation/result.json` PASS for full existing regression. Added WARP_ROTATION_PROBE to the verifier and executed it against the completed live engine (`build/warp-rotation-angle-probe.json`, ok): quarter-turn timing, noncommuting quaternion endpoint, and six real traversals at +/-15 degrees across three heights verify target rotation, wrist contact and exit. Engine-owned `angled-contact.png` inspected. BackLedge/BackFloor graph-derived target creation/orientation still needs full audit; moving target full rigid transforms, post-contact endWarp removal, speed policies and schema-specific matching remain incomplete. The source architecture check is tracked in `build/warp-rotation-architecture.log`; do not infer full parity from these bounded tests.
- Step 5 moving-window targets now store root target position and rotation relative to the course-owned moving body, and resolve both against its current rigid transform at every extraction. This replaces height-only target following inside warp windows. No new ownership/handle/API/schema: the action remains course-owned and reset before course teardown. Legacy post-contact endWarp still follows height only and is explicitly pending removal; BackFloor ownership/target-creation parity also remains pending.
- `build/traversal-rigid-target/result.json` PASS for full regression including six angled traversals. Added DYNAMIC_WARP_PROBE, then ran it on the live engine (`build/rigid-target-live-probe.json`, ok; REFERENCE_DYNAMIC_WARP_PASS frames=20): moving platform translates up to 12 cm sideways/8 cm forward while yawing up to 10 degrees; target rotation and wrist-in-platform coordinates verified at first window end. The first probe omitted Standard-entry transition time and failed visually; corrected the harness to execute world, action, events, root offset and transition timing in normal order, preserving contact bounds. Engine-owned `rigid-contact.png` inspected. Architecture source gate PASS (`build/rigid-target-architecture.log`), diff check PASS. This is bounded yaw/translation coverage, not arbitrary pitch/roll or whole-action moving-contact fidelity. No deliberate architecture exception. Next remove post-contact interpolation with source BackLedge/BackFloor geometry, then finish curve boundary and schema-specific matching work.
- Step 5 authored tail: removed the entire legacy Mantle endWarp position interpolation and its separate height-follow addition. Window splitting now naturally passes subsequent authored translation/rotation through the same incremental extraction, seeded by collision-resolved root. Removed unused fitTime/landingTarget/platformY state. UE ProcessRootMotion explicitly extracts ExtraRootMotion after EndTime; no source basis existed for the removed landing-point blend.
- `build/traversal-authored-tail/result.json` PASS, including six angled traversals, rigid moving-target contact, repeated traversal/contact, moving support, high-platform falling and conditional/natural exits. Added AUTHORED_TAIL_PROBE and ran live (`build/authored-tail-live-probe.json`, ok): at three heights the post-last-window request equals the rotated source delta from the actual origin. Engine-owned contact and conditional-exit frames inspected. No public API/schema/owner change, no architecture exception. Source gate tracked in `build/authored-tail-architecture.log`. Remaining step 5: source BackLedge/BackFloor graph geometry/ownership, zero-translation exact policy and speed settings; step 4 trace/curve boundaries and step 6 schema-specific matching remain incomplete.
- Step 5 source SetWarpTargets graph decoded with node/pin links (`build/warp-target-pin-map.txt`). FrontLedge alone connects MakeRotFromX(-FrontLedgeNormal); BackLedge/BackFloor rotation pins are unconnected identity. BackFloor horizontal target is BackLedgeLocation + BackLedgeNormal * abs(Distance_From_Ledge at first BackLedge window end minus that at first BackFloor window end), with height taken from detected BackFloorLocation. Targets are conditionally removed when action/window conditions fail. Current fixed-distance BackFloor geometry is therefore still incomplete and curve sampling/import must follow; do not describe it as source equivalent.
- Corrected BackLedge/BackFloor initial rotation to identity. Floor query now records whether the floor is the moving support independently of the obstacle; only targets actually on that support attach to its transform. This is the example's moving-platform adaptation; source graph uses location/rotation targets, not component-bound targets. Full `build/traversal-target-ownership/result.json` PASS. Added TARGET_POLICY_PROBE and ran live (`build/target-ownership-live-probe.json`): independent floor stays fixed as obstacle translates/rotates, identity rotation retained, moving-floor attachment exists. No API/schema/new lifetime owner or deliberate architecture exception. Gate tracked in `build/target-ownership-architecture.log`. Next implement the actual distance-curve-derived BackFloor and conditional targets, then remaining step 4/6 work.
- Step 5 distance data evidence: executed UE AnimationWarpingLibrary.GetCurveValueFromAnimation on actual montages at each first BackLedge/BackFloor window end. This API resolves the montage segment and converts track time before curve evaluation. New reusable `tools/unreal-uasset-converter/ue_export_warp_distances.py` exports `eve.unreal-warp-distance-samples/1`, selection hash, montage/sequence hashes, sample times and explicit curve presence; absent curves retain UE's zero OutValue. Atomic replacement preserves old output on malformed/nonfinite data. Two focused tests PASS.
- Real production exporter commandlet completed exit 0 (`build/warp-distance-production-export.log`): `build/ue-reference/warp-distance-samples.json` has 34 montage records, 21 evaluated samples, 22,650 bytes. Thirteen BackFloor distances range 55.6969147 to 118.0687943 cm after subtracting first BackLedge curve value (zero when no BackLedge window). Confirmed E_TraversalActionType: NewEnumerator1=Vault, NewEnumerator2=Hurdle; source creates BackFloor only for Hurdle, BackLedge for Vault/Hurdle. These samples are staged, NOT consumed by runtime yet. Next version the selection contract, verify hashes/window times during compilation, implement source conditional target creation and distance-derived BackFloor. Architecture/fixtures tracked in `build/warp-distance-architecture.log` and `build/warp-distance-fixtures.log`; no public API/lifecycle change or deliberate exception.
- Step 5 distance integration: selection schema5 adds per-montage warpDistances in metres, retaining sampled time/presence. Pure attach_warp_distances validates exact montage coverage, source montage/sequence hashes against imported catalogue, first-window times, presence and finite values before returning new records. Runtime rejects schema4 and validates distance target/time coverage. All34 montages regenerated from real UE samples. Exporter accepts schema4/5 for bootstrap/migration. README documents regeneration. One initial restricted write to the shared generated-asset directory failed; authorized elevated compiler run succeeded without changing source UE packages.
- Runtime Hurdle BackFloor horizontal coordinates now use BackLedge + back-face normal * abs(first BackLedge distance or zero - first BackFloor distance), retaining queried floor height. Source action conditions enforced: BackFloor only Hurdle; BackLedge Vault/Hurdle. Back-face normal captured before subsequent floor ray overwrites query results. Full `build/traversal-distance-schema5/result.json` PASS; engine-owned hurdle-landed frame inspected. Latest runtime coverage validation executed live and rejects altered first-window time (`build/distance-schema5-validation-probe.json`, ok). 25 export/compile tests PASS; prior architecture fixtures9PASS191.188s, current source gate `build/distance-schema5-architecture.log` PASS. Persistent-format version/unknown fields/source provenance/atomic runtime replacement rules apply, no deliberate exception. Remaining: zero-translation/speed policy fidelity, foot trace/curve boundary parity, and schema-specific matching; full six-item objective remains active.
- Step 5 zero-translation branch now uses the eased step magnitude directed toward the current target, matching UE's no-authored-translation branch; a reached target returns zero instead of being pulled back to the original interpolation line. Added authored-speed ratio limiting for translated window segments, with 3D and planar variants (planar preserves vertical delta). These are private pure helpers with explicit input/time, no API/schema/lifecycle change or deliberate exception.
- `build/traversal-warp-policies/result.json` PASS for full real-engine regression. Added focused helper cases to the verifier, then executed live (`build/warp-policies-live-probe.json`, ok): off-line actor direction and eased length, reached target, 3D clamp, planar clamp preserving vertical movement, disabled clamp. Engine-owned moving-platform contact frame inspected. Current source gate tracked in `build/warp-policies-architecture.log`. Actual imported windows use ratio0, so enabled-cap evidence is the focused helper checks; boundary frames spanning EndTime still need source comparison because UE clamps window+ExtraRootMotion together whereas this runtime splits intervals. Do not mark step5/all6 complete yet. Remaining step4 trace/curve boundaries and step6 schema-specific matching are unchanged.
- Step 4 exit curve lifetime fixed: an advancing montage exit tail now contributes curve presence for its actual blend duration, rather than losing it when the shorter generic inertia duration expires. Synthetic runtime coverage extends CONTACT_CURVE_PROBE with a .6 s outgoing curve tail into a clip with no curves: presence and actual blended value verified after .4 s, and full removal after completion. `build/traversal-contact-tail/result.json` PASS for the entire real-engine suite with this expanded probe; conditional exit frame inspected. No API/schema/lifecycle change or deliberate exception; source gate tracked in `build/contact-tail-architecture.log`.
- Missing-contact source audit resolved: actual SandboxCharacter_CMC_ABP RemapCurves expression is contact_l=(1-contact_l)*100 (and right). AnimNode_RemapCurves evaluates missing SourceCurve.Get(name) as zero and writes the finite expression result, so absent contact becomes present speed100 before FootPlacement. Hence current no-contact alignment0/unplanted behavior is correct for this graph; do NOT replace it with FootPlacement's isolated missing-speed fallback1/alignment1. Remaining foot parity: sphere/complex tracing, air physical-plane handling and exact pelvis behavior. Step6 schema matching and step5 cross-window enabled-cap parity still pending.
- Step4 foot tracing now uses existing World3D.castSphere, source default radius .05 m and vertical sweep from +.75 to -1.0 m, rather than the old +.20/- .50 centre ray. Reads surface impact point/normal and retains walkability rejection. Source T3D overrides only MaxGroundPenetration, so trace extents/radius come from UE FootPlacement header defaults. No new native API/schema/lifecycle or deliberate exception; existing physics sphere casts support mesh/heightfield shapes, though source separate simple/complex channel blending remains to audit.
- `build/traversal-foot-sweep/result.json` PASS for full real runtime regression. Added flat-height/edge probe and ran live (`build/foot-sweep-edge-probe.json`, ok): surface height equals box top (not sphere centre); sphere finds walkable edge 2 cm beyond box where centre ray misses. Repeated sloped support test (`build/foot-sweep-slopes/responses.json`): static180/180, rotating180/180,720samples,minclearance .0268307 m, walking .616666 m; close walking frame inspected. Architecture source gate PASS (`build/foot-sweep-architecture.log`), diff check PASS. Remaining step4: air physical-plane/virtual-plane and pelvis semantics; step5 boundary-cap policies and step6 schema-specific matching still pending.
- Step4 air physical-plane handling: source UpdatePlantingPlaneInterpolation always traces, then replaces desired plane with IK-root virtual plane in air; its interpolation penetration bound still uses the physical impact when found. Runtime now also queries actual ankle/ball support in air, passes it separately to floor smoothing, and bounds the smoothed height to physical plane minus .20 m. Ground/plant eligibility remains disabled in air, so detecting a nearby ledge does not itself lock a foot. No public API/schema/lifecycle change or deliberate exception.
- `build/traversal-air-contact-plane/result.json` PASS for full real-engine regression, including three-height contacts, moving targets and high-platform falling. Expanded pure plane probe passed live (`build/air-contact-plane-live-probe.json`): no-ground virtual-plane convergence, actual surface penetration bound while airborne, and recovery when that surface disappears. Engine-owned high-platform-falling frame inspected. Current source gate tracked in `build/air-contact-plane-architecture.log`. Air pelvis semantics, missing-hit floor fallback/complex channels, step5 enabled-cap frame boundaries and step6 schema matching remain incomplete.
- Air pelvis continuation is an UNVERIFIED candidate: removed blanket !grounded pelvisTarget/planar zeroing after confirming bDisablePelvisOffsetInAir only gates actor-motion compensation in UE UpdateCharacterData. `build/air-pelvis-verifier.log` FAILED low mantle contact (wrist y=.710 z=4.650 exceeded existing bound); do not relax assertion or report this candidate fixed. Source gate passed (`build/air-pelvis-architecture.log`), runtime remains failing. Latest verifier process81400 completed/failed; it kills its owned engine on failure. Earlier kept-running engines may remain: before next launch enumerate by exact workspace eve.exe path and stop those, preserving other worktree binaries.
- Further direct source evidence changes the next fix: SolvePelvis uses full AlignedFootTransformRS. AlignPlantToGround always writes corrected transform, without AlignmentAlpha weighting. AlignmentAlpha appears in planting/time-since-unaligned and heel-lift behavior, not a generic ground-alignment blend. Current reference_foot_placement preblends target with align, fraction-blends rotation, and uses s.weight for IK, so ground-alignment and contact/lock weights are conflated. Inspect FinalizeFootAlignment output/DisableLeg flow and separate these weights before finishing air pelvis. This also revises earlier missing-curve conclusion: absent remapped contact indeed becomes speed100/unplanted, but that does NOT by itself prove whole ground alignment must be zero. Need exact output-stage source audit. Keep all six goals active; no architecture exception claimed and no passing runtime evidence for current candidate.
- Continued full-alignment candidate: confirmed FinalizeFootAlignment only blends by DisableLeg (source default NAME_None), not AlignmentAlpha. Runtime no longer fraction-blends aligned rotation, preblends pelvis targets, or attenuates final leg IK by contact weight; contact weight still drives heel correction and unplant timing. This is NOT validated: `build/full-foot-alignment-verifier.log` fails six-angle contact probe (hand y=.7212 z=4.64399). Preserve existing assertions. No final fix claimed.
- Stopped all eve processes whose executable exactly matches this worktree before the run; preserved other worktree binaries. Failed verifier94518 exited and killed its engine. Started one diagnostic engine PID36000, MCP57450, stdout `build/foot-alignment-diagnose.log`, stderr `build/foot-alignment-diagnose-error.log`; it remains running. Live wrapper temporarily recorded referencePelvisRange arguments/results then restored function/update and reset course. PELVIS_DEBUG shows low mantle pelvis offset .06239 at time.3833, .10204 at .4667, .10114 at .55; hand correspondingly .72876/.77648/.77977. Actual capsule feet remain at ground0 through .55 due floor adaptation. Need resolve reference-plane/physical-floor/root-lowering interaction rather than hide with air-zeroing or loosen bounds.
- Source graph ordering confirmed with pins: Slot_0 -> OffsetRootBone_0 -> RemapCurves_0 -> LocalToComponentSpace_0 -> FootPlacement_0 -> LegIK_1 -> ComponentToLocalSpace_2 -> PoseSearchHistoryCollector_0 -> Root_0. No post-foot hand IK in this path; do not invent graph parity. Also source Bones.LimbLength and FootLength are cached from reference skeleton, whereas runtime recomputes from current pose; audit/fix that next alongside plane/root adaptation. Entire six-item objective remains active; latest runtime candidate is failing.

- Root-space plane transport now carries the previous plant plane and last aligned foot through the input root's rigid transform; the world/support plant anchor remains independently owned. Reference limb/foot lengths are cached from bind pose. Expanded FOOT_PLACEMENT_PROBE verifies translation, quarter-turn rotation, unchanged world anchor, and repeated-evaluation idempotence. Fresh engine diagnostic plus all FOOT_PLACEMENT_PROBE checks PASS (`build/root-plane-transport-proof.log`, DIAGNOSTIC_DONE); this is narrow evidence, not full traversal success. No public API/schema/lifecycle change or architecture exception.
- Full `build/root-plane-verifier.log` remains FAILED low contact y=.713 z=4.648. New authoritative per-frame `build/root-plane-diagnosis/runtime.log` identifies the remaining mechanism: visualRootLowering reaches -.375 m, authored IK-root follows it, but the physical penetration bound holds plant plane at -.20 m. At montage .40 s pelvis offset=.04294 m, hand=.71304 m; at .50 s pelvis=.09484 m, hand=.77218 m. Plane transport fixes coordinate drift but cannot remove this physical-plane clamp. Actual exported node overrides MaxGroundPenetration=20 cm, confirmed; do not change it to hide this conflict.
- Source PSS_Traversal audit confirms current entry scoring is incomplete: source includes attach position AND heading (weight15 each), left-foot relative right-foot position/velocity, individual vertical foot velocities (weight.3), and past trajectory. Runtime uses attach position plus foot/pelvis position errors, with no heading/velocity/normalization. Its query still uses inset topPoint while execution uses front edge. Resolve actual Get_InteractionTransform graph and source selection semantics before choosing a workaround to low-ledge root lowering. Also current keepTakeoffFeetAboveGround solves FK before full FootPlacement solves from unchanged IK references; inspect this duplicate correction while fixing pipeline. All six goals remain active; no final visual success claimed.

- Corrected traversal search position to frontPoint XZ + confirmed top Y (no .005 execution clearance), replacing inset topPoint XZ. Direct pin trace: AC_TraversalLogic.TryTraversalAction.K2Node_CallFunction_17 Set_InteractionTransform <- CallFunction_31 MakeTransform; Location <- BreakStruct_0.FrontLedgeLocation, Rotation <- CallFunction_3 MakeRotFromZ(FrontLedgeNormal). PSC_Traversal_Pos/Head both consume that interaction transform. Thus the heading query must not be copied from execution MakeRotFromX(-normal).
- Fresh `build/front-query-hands/runtime.log` selects AM_M_Neutral_Traversal_Mantle_1_0_stand_F_Rfoot, start .1. Both wrists recorded: time .46667 left .85805/right .75463; time .56667 left1.21455/right.75264, pelvis .08205. Real diagnostic and FOOT_PLACEMENT_PROBE complete (`build/front-query-hands.log` DIAGNOSTIC_DONE), but contact remains wrong; do not report full regression success. Selection changed because source position now differs from the old inset query; missing source heading/relative feet/velocity/normalization must be implemented rather than reverting a source-correct query to favor old assertions. No API/schema/lifecycle change, no deliberate exception; git diff --check passed. Diagnostic process21641 terminated normally, no kept-running engine from these diagnostics.

- Added traversal attach heading feature using source HeadingAxis::X and weight15. Fresh read-only UE commandlet loaded the actual PSS_Traversal and exported effective inherited properties (`build/traversal-effective-schema.json`, `build/effective-schema-export.log`, exit0). This confirms PSC_Traversal_Head_C HeadingAxis.X, None stripping, zero offsets, weight15. Source GLTFCoreUtilities::ConvertVector maps UE(X,Y,Z) -> (X,Z,Y); ConvertRotation=(-qx,-qz,-qy,qw). Source TRotationMatrix::MakeFromZ uses Up cross Normal and the near-vertical fallback. Runtime helper accounts for handedness and transforms query into character heading space; candidate uses root-relative attach quaternion X axis.
- `build/traversal-heading-proof.log` DIAGNOSTIC_DONE, fresh engine run including newly added FOOT_PLACEMENT_PROBE heading checks: joint quarter-turn invariance, front-face expected axis, vertical fallback, degenerate normal rejection. Existing foot probe also passes. Low-lane diagnostic still selects Rfoot/start.1 and contact/pelvis numbers are unchanged; heading addition alone does not fix low mantle and full traversal remains unproven/failing. Do not claim matching parity: current cost lacks source group normalization, relative-foot position/velocities, vertical velocities and past trajectory. Raw feature weights remain intermediate until that normalization is implemented. No API/schema/cross-domain lifecycle edit or deliberate architecture exception; diff check passed. UE source assets not saved.

- Replaced ad-hoc independent foot/pelvis position penalties with PSS_Traversal's left-foot-relative-to-right-foot position channel, weight1. New referenceTraversalRelativePosition subtracts the origin bone and unrotates by root quaternion, matching UE FAssetIndexer::GetSamplePositionInternal / FSearchContext::GetSamplePositionInternal (non-root origin case). No pelvis position channel exists in this schema. Fresh `build/traversal-relative-feet-proof.log` DIAGNOSTIC_DONE includes expanded FOOT_PLACEMENT_PROBE: whole-pose translation/root-yaw invariance and sensitivity to actual foot displacement. Diagnostic low lane still chooses Rfoot/start.1 and wrist/pelvis offsets unchanged; this is not full contact success.
- Velocity source audit: UE FAssetIndexer::GetSampleVelocity uses backward difference at FiniteDelta=1/60 (PoseSearchDefines.h); when bUseCharacterSpaceVelocities=true it shifts both sample and origin time. Existing referenceState.poseHistory already owns prior rendered pose for matcher query (copied before matcher.update each locomotion step). Before adding a second history, assess reuse and ensure it does not stay stale through traversal/air exits. Current step lacks velocity channels, past trajectory and normalization. No public API/schema/cross-domain lifetime changes or exceptions this turn. All six remain active; no full regression pass claimed.

- Added PSS_Traversal character-space velocity channels: left relative right (all3,weight1), left/root vertical and right/root vertical (weight.3 each, UE Z->runtimeY). Candidate samples use source finite backward delta1/60s, previous clip sample at nonnegative time; query uses existing referenceState.poseHistory. Root-relative vector helper is shared with position channel.
- Moved poseHistory advancement from locomotion-only matching into positive-dt applyCharacterPose before overwriting renderPose. This preserves a single history owner and captures the preceding rendered pose during traversal too; dt0 does not advance it. Existing reset/bank-switch invalidation remains. No new persistent schema/public API/cross-domain lifecycle or exception.
- `build/traversal-velocity-history-proof.log` DIAGNOSTIC_DONE (fresh engine): added checks verify .1m/1-60s displacement has 6m/s magnitude, missing history yields zero velocity, preceding rendered pose retained for six active traversal frames, dt0 history unchanged; existing FOOT_PLACEMENT_PROBE checks also passed. Source contract gate PASS `build/traversal-velocity-architecture.log`.
- Full `build/traversal-velocity-regression.log` FAILED at earlier six-angle traversal hand probe (.792024,4.49198); this occurs before FOOT_PLACEMENT_PROBE so the dedicated run above is its evidence. Diagnostic selects Rfoot/start0 now, compared with prior start.1; low contact remains wrong. Full task is not completed. Next source normalization evidence: PoseSearchDerivedData.cpp FMeanDeviationCalculator groups CanBeNormalizedWith channels, computes shared vector centroid then mean Euclidean distance (not per-axis standard deviation), and uses1 for deviation<=.1 UE units. Need apply true source groups/index population, plus past trajectory, before claiming traversal matching parity. Active verification handles73844/71110/16801 all terminal; diagnostics own engines terminated normally.

- Corrected active schema provenance: CHT_TraversalMontages_CMC's PSD_Mantles_0/PSD_Hurdles_0/PSD_Vaults_0 use PSS_Traversal_Chooser, not PSS_Traversal. Exported active leaf feature layout is the same six pose channels plus trajectory, but use the actual Chooser schema for future effective-property export. PoseSearch columns retain per-row ranges and each embedded DB indexes its full enabled table before runtime filtering.
- Added reference_traversal_features.nut: transient per-referenceBank/per-table feature cache contains all compiled montage rows/ranges, sampled features in UE centimetres, and independent channel mean deviations. Source CanBeNormalizedWith requires cardinality/class and matching group or label; the six current pose channels do not share groups (only left vertical explicitly names FeetVelZ). FMeanDeviationCalculator uses mean Euclidean distance to common centroid, fallback1 below/equal.1 source units. Cost applies squared inverse deviation and per-component source weights normalized by96.6 for these six channels. Trajectory channels remain missing; full schema weight sum must be extended with them. This is not complete matching parity.
- selectReferenceTraversal now searches cached eligible rows, rather than resampling and accumulating unnormalized errors every request. Internal chooser results retain table identity. Query attach position is additionally transformed by the actual rendered root, matching source query frame; no serialized format/public API change. Bank owns cache; replacing bank discards it; reset-course can reuse immutable sampled data. No cross-domain lifecycle change or deliberate exception.
- `build/traversal-normalization-proof.log` DIAGNOSTIC_DONE: fresh engine includes Euclidean-centroid/deviation, translation-invariance, small-deviation fallback, squared normalized weight formula, all-table-row coverage and same-cache reuse checks plus existing foot/history probes. Source gate PASS `build/traversal-normalization-architecture.log`; diff check PASS. Full `build/traversal-normalization-regression.log` FAILED six-angle contact (.792046,4.49198). Low diagnostic still selects Rfoot/start.1, right hand ~.75 for .62 ledge. No success claimed for contact. All run handles52206/96041/3216/10810 are terminal, verifier killed its own engine.
- Next: source past trajectory channels/index sample-grid/range population need completion. Also existing contact verifier hardcodes left hand, while new source selection picks Rfoot variants; inspect source hand-to-attach/contact timing before interpreting left-hand checks as universal. Do not loosen contact bounds to pass; right-hand low-lane data already shows a remaining real offset. Preserve six-item goal and remaining root/pelvis contact work.

- Added both active PSS_Traversal_Chooser trajectory channels: root position at -.4 s (planar,weight.5) and current root velocity (planar,weight1,bUseCharacterSpaceVelocities=false). Cached feature groups now eight, source per-dimension weight sum99.6. Query rendered-root history records after FootPlacement every positive simulation dt, seeds once at dt0, bounded ~.5 s; unlike existing capsule positionHistory, it includes root offset and active traversal. Selection state owns copied points/time and resets with course. Same-time external reposition reseeds instead of fabricating velocity across teleport. Source pose-history non-extrapolating boundary behavior is retained by holding first/last sample.
- New reference_pose_history.nut samples montage root before time0 using source first1/30s rigid root delta repeated/inverted plus fractional interval. Root-space trajectory velocity uses current root frame (does not cancel character motion). Foot derivative prior sample is now clamped to montage start+offset rather than sequence time0. No native/public API/persistent schema/cross-domain owner change or exception.
- `build/traversal-trajectory-proof.log` DIAGNOSTIC_DONE: interpolation/edge clamp, cm and cm/s units, rotating inverse root extrapolation (two full intervals + fractional), actual root capture through active traversal, dt0 idempotence, teleport invalidation, prior foot/normalization checks. Architecture source PASS `build/traversal-trajectory-architecture.log`. Full `build/traversal-trajectory-regression.log` FAILED six-angle hand (.792046,4.49198); current real contact remains unresolved.
- Further source fix: FSearchIndexAsset constructor uses ceil(rangeMin*30) and floor(rangeMax*30), so sampling is global-grid anchored. Removed ad hoc montage.blendOut-.1 cutoff; use actual row range bounded by montage duration. `build/traversal-sample-grid-proof.log` DIAGNOSTIC_DONE verifies .88-.91 maps only sample27 and0-.08 maps0..2, plus all trajectory tests. Low lane still Rfoot/start.066667; wrist heights remain wrong. Diff check PASS. All run handles9795/42104/57577/3963/95202 terminal and owned engines stopped. Full architecture source gate preceded the final pure sampling-grid edit; no refactor/API change in that edit.
- Still pending: exact schema/column import (currently active eight-channel layout is coded from source), notify-based sample exclusions/database pruning or normalization-set scope, pose reselect history, and non-traversal per-Schema matching. More urgently verify actual support hand/timing for selected Rfoot montage and root/pelvis conflict without weakening bounds. All six objective items remain active; eight channels implemented is not proof of original visual coordination or completed contact fixes.


### Contact audit and moving-support correction

- Fresh source/pre-FootPlacement/final-pose diagnostics (`build/contact-fingers/runtime.log`) resolve the earlier left-wrist failures: the selected Rfoot montage first reaches its attach target at .44961 s, then plants the right fingers. The left hand is authored to lift. First-window attach arrival and later physical finger contact are separate assertions; fixed-left-wrist assertions were invalid for this montage. The verifier now checks both actual fingers against their pre-foot authored pose and the physical surface, requiring consecutive eligible contact frames so a no-contact run cannot pass.
- A real low-ledge error remained: at .58333 s, right middle fingertip pre-foot Y=.63353 on a .62 m ledge became final Y=.72128 because floor adaptation raised the pelvis. New private `reference_hand_contact.nut` limits pelvis displacement by the supporting arm's bind-pose reach and restores the authored wrist transform after FootPlacement. It uses current pre-foot fingertips, traced obstacle identity, a short weight transition and actual FrontLedge release; it never pins the lifted hand or changes capsule/root authority. This is an explicit adaptation for this example's collision/floor correction, NOT a node found in the source UE graph.
- Coupled pelvis/arm diagnostic (`build/hand-reach-pelvis/runtime.log`): at .58333 s final fingertip Y=.63855; at .60 s final Y=.63444 versus authored .63385. Arm-only IK was insufficient because the raised shoulder put the target outside arm reach; the coupled constraint resolves that cause.
- `build/hand-contact-twelve-proof.log` PASS: three static heights and moving support, each at -15/0/+15 degrees; physical fingertip clearance <=.035 m, authored position deviation <=.025 m, non-vacuous contact coverage, dt0 re-evaluation idempotence and release/reset. Moving cases explicitly seed support pose/time; resetCourse intentionally resets the character, not the world animation. The previous first-moving-case entry failure came from inherited platform phase, not from finger IK.
- Full `build/hand-contact-final-regression.log` FAILED moving-platform attach retention after passing ordinary movement, jump/slide, three traversals, repeated traversal, collision, Hurdle and Vault. Diagnostic `build/moving-contact-phases/runtime.log` confirms a real missing transport: at .45 s attach error is zero, then -.03399 m vertically at .60 s while support rises. Updating the target alone cannot carry a held source root with zero extracted displacement. This diagnostic only completed phase0; its next phase was outside the entry probe and is not evidence for four phases.
- Added support-frame transport after first FrontLedge contact until final FrontLedge release. Copied previous support transform transports the collision-resolved extraction origin and rotation; the resulting motion request still passes through advanceWarped's capsule sweep. Positive-time evaluation updates the support snapshot; same-time cached evaluation cannot accumulate movement. Existing traversal reset/world teardown drops this private state; no Body pointer or second capsule-position authority is introduced. This extends the example's moving-support behavior and is not claimed to reproduce a specific source Blueprint node.
- `build/moving-frame-contact-proof.log` PASS for all twelve actual hand cases and the existing foot/history/normalization probes after support transport. Full regression, including a new eight-frame translated/rotated held-contact test, is running in `build/support-frame-final-regression`; do not claim complete pass until terminal result. Source architecture gate passed before support transport (`build/hand-contact-final-architecture.log`); rerun after final edits. No public API, persistent schema, module boundary or deliberate architecture exception in these script changes.

### Remaining matching differences confirmed in exported reference assets

- Current ordinary matcher still uses a fixed 30-component encoder and five horizons (-.05,0,.35,.7,1 s), regardless of selected database schema (`MotionFeatureInternal.h`, `reference_selection.nut`). Correction after effective-property audit: PSS_Default has full XYZ foot velocities (ComponentStripping=None), despite the FeetVelZ normalization label. The existing native 30-component default layout is consistent with these axes; only the traversal schema strips horizontal foot velocity. Do not shrink the default layout based on that label.
- Actual `PSS_Jump` trajectory samples are -.2,0,.2,.5 s, with different position/velocity/direction flags. `PSS_Relaxed_Jump` uses -.2,0,.25,.5,1 s. Reusing the ground horizon/vector layout cannot reproduce these searches or their landing preference.
- Actual `PSS_Relaxed_Loops` samples -.125,0,.33,.8,1.35 s and includes two Phase curve channels (one past sample), while current matching has no Phase channel. `PSS_Idle` includes a Pose subchannel; `PSS_Stop` changes current trajectory flags and omits the default 1 s sample. These are directly observed differences in local T3D exports, not assumptions about clip quantity.
- Traversal now has the active eight-channel source layout and mean-deviation normalization, but the layout is still coded from source. Exact data-driven schema import, common-schema/normalization-set scope, notify exclusions, reselection history and ordinary-schema encoders remain unfinished. Root-offset/orientation and pelvis actor-motion policies also retain known approximations. All six objective items remain active; these contact fixes alone do not establish original visual coordination.

- Final fresh `build/support-frame-final-regression/result.json` PASS for the complete verifier, including twelve finger-contact cases, translated/rotated held contact, dt0 support idempotence, 24 consecutive traversals, Hurdle/Vault, moving contact/release, high-platform movement/falling and collision/render authority. `build/support-frame-final-architecture.log` PASS after support transport. These are bounded regression checks, not proof of complete six-item source parity.
- Final engine-owned close side frame inspected: `build/hand-contact-close-final/contact-side.png` at montage .60 s shows the supporting hand at the low ledge; HUD partly overlaps the upper body, but the contact is unobstructed. Camera override used resolveCourseCamera because eve_render rewrites Camera3D each frame. Earlier direct-camera screenshot was a distant view and was not accepted as close-contact evidence. Diagnostic exited normally and stopped its owned engine. All current verification sessions are terminal; no engine was intentionally left open.


### Effective schema extraction (step 6 continues)

- Previous turn was progress: private hand/support fixes and full55-check runtime evidence. This turn preserves that verified runtime and advances the authoritative data needed for schema-specific indexing rather than guessing a new feature layout.
- Added standalone `tools/unreal-uasset-converter/ue_export_pose_schemas.py`. Reads all PSS assets using actual UE reflected properties; full paths preserve hierarchy/identity, required fields fail explicitly, primitive/enum/bone values are typed, nonfinite values fail. Group children retain order; Pose and Trajectory retain ordered authored samples because this installed editor does not reflect Pose.SubChannels despite its presence in the local source header. Eight source channel families include custom Blueprint subclasses; class paths remain explicit for later query-override routing.
- Output `eve.unreal-pose-schemas/1` is UE-centimetre data with source-package SHA256, unknown-field rejection, explicit re-export migration and atomic publication after complete success. No source package saves, retained UE object pointers, native API, module/lifecycle changes or architecture exception. This is channel configuration, not complete database/filter/normalization/Blueprint behavior.
- Fresh UE commandlet `build/pose-schema-export-final.log` exited0 and produced `build/ue-reference/pose-schemas.json` with34 schemas. Confirms PSS_Default foot velocities ComponentStripping=NONE; active PSS_Traversal_Chooser uses STRIP_XY. Prior suggestion to shrink default XYZ features was wrong and no such code change was made.
- Also confirmed existing MotionMatcher::search already replaces pose query components with eligible continuing pose values while retaining character trajectory. Do not reimplement this or call it wholly missing. Source CanUseContinuingPoseValues checks valid continuing result and identical schema; precise cross-schema handling remains for the actual multi-schema implementation.
- Attempted integration into old ue_export_selection failed BEFORE schema extraction because the source now contains duplicate short-name PSD_Dense_Stand_Walk_Stops assets (`build/pose-schema-integrated-export.log`, exit1). Preserved the existing ambiguity rejection and removed the attempted integration; the new standalone schema command uses full paths and successfully reads all34. Do not claim the old flat T3D export pipeline passed or bypass the ambiguity by overwriting files. Future full-database regeneration must resolve package-path identity explicitly.
- Five isolated exporter tests PASS: effective axes vs group label, repeated-name hierarchy, authored Pose/Trajectory samples without transient children, missing/nonfinite fields, and injected publish failure preserving old output/no temp leakage. Architecture source gate PASS (`build/pose-schema-architecture.log`); all9 architecture fixtures PASS (`build/pose-schema-architecture-fixtures.log`, 140.892s). The source gate preceded the final source-hash/doc-only additions; isolated exporter tests passed again after those changes. Runtime changes were not made this turn; previous55-check engine result remains the last runtime evidence. Runtime schema compilation/routing, Phase extraction, distinct trajectory encoders and normalization-set scope remain unfinished; all six objectives stay active.


### Schema compilation and traversal consumption (selection v6)

- Previous goal turn was progress (34 effective source snapshots and tested atomic export). Current turn adds `scripts/unreal_pose_schemas.py` and connects it to the real selection compiler. Source Pose/Trajectory Finalize expansion order is preserved: position, two heading vectors for rotation, then velocity; trajectory position/velocity/normalized-velocity/heading. UE axes map to engine XYZ explicitly, sample weights multiply their parent weights, zero-weight channels retain dimensionality, and query/normalization/Blueprint identity is retained. Unsupported roles, computed bone-phase flags, sampling attributes, group subclasses and unknown custom query implementations reject rather than silently becoming default features.
- The 34 source files represent33 resolved schemas: PSS_Relaxed_Trans redirects to PSS_Relaxed_StandTurn. Snapshot v1 conflated the requested and resolved identity. `eve.unreal-pose-schemas/2` records source_path plus resolved object path and hashes both packages; consumers reject v1 and require re-export. Source and resolved packages are checked for modification around extraction. Fresh `build/pose-schema-v2-export.log` exited0. No source package was saved. Current compiler keeps request identity and resolved identity separate and hashes each canonical layout deterministically.
- Source compilation resolves all16 distinct schema paths referenced by the106 imported databases plus the actual embedded traversal schema (17 layouts). Representative dimensions: Default30, Idle34, Jump39, Relaxed_Loops25, Stop30, active Traversal_Chooser18. This is data, not yet proof of ordinary-schema runtime encoding. Some source trajectory parent weights are large; preserve those effective values rather than flattening all groups to1.
- `build_unreal_selection.py` now reads the schema from each PoseSearchColumn row's embedded database, records tableSchemas, and emits selection schema6 with compiled layouts. It requires the effective snapshot and hashes it. All21 tables,106 databases and34 montages regenerated successfully. The runtime rejects selection5 and unknown fields; regeneration is the explicit migration. It validates layouts before publishing the replacement bank. Existing selection.json is a companion report; selection.nut is the runtime's self-contained authoritative import.
- New `reference_feature_schema.nut` resolves the existing eight traversal encoder slots by semantic channel identity, independently of serialized order. Weights, total dimensional weight, schema sample rate, past-root query time and explicit normalization groups now come from imported data. Unsupported traversal pose offsets, origins, axes or extra channels reject before bank publication. Bank-owned immutable configuration determines retained root-history duration; reset continues to invalidate copied history. No native API/module/cross-domain lifecycle change or deliberate architecture exception.
- Traversal indexing and query now use the imported sampling grid/historical offset; scoring uses imported weights. Normalization pools explicit same-class/cardinality groups and retains distinct semantic labels for the implemented traversal channels. This does NOT yet establish cross-database NormalizationSet/common-schema pooling or support arbitrary pose schema channels in the ordinary native matcher.
- First runtime attempt failed initialization because an absolute1e-5 weight-sum check was too strict for float32 accumulation of large source group weights; the later drainEvents error was downstream of that failed initialization. The validator now uses1e-6 relative numeric tolerance and includes the schema path in mismatch diagnostics. `build/schema-driven-traversal-proof2.log` PASS for actual contact and existing pose/trajectory probes.
- Added eight isolated compiler tests (trajectory flags/order/weights, two-axis pose rotation, zero-weight dimensions, redirects and duplicate names, deterministic hashing, input immutability, unknown-field/missing-schema/invalid-value rejection). Together with existing selection and exporter tests:30 PASS. Expanded real-engine FOOT_PLACEMENT_PROBE changes imported weight, sample rate and normalization group to prove runtime consumption, rather than only testing the original constants.
- Full `build/schema-selection-v6-regression/result.json` PASS55 checks, including those new parameter checks, twelve hand contacts,24 consecutive traversals, moving/rotated support, conditional exits, high-platform movement/falling and collision/render authority. Engine-owned static and moving contact screenshots inspected; these are distant representative frames, not new close-up finger evidence. Verification engine exited normally and was stopped. Architecture source gate PASS (`build/schema-selection-v6-architecture.log`) and all9 fixtures PASS (`build/schema-selection-v6-fixtures.log`,147.942s); both handles terminal. Compiled selection coverage also verifies every database and all3 embedded traversal tables resolve to an imported layout.
- Remaining step6: replace the ordinary fixed native30-component layout with actual per-schema indexing/query routing (especially Idle34, Jump39 and Phase-curve schemas), import/evaluate Phase curves, and establish source normalization-set, notify-exclusion and reselection-history semantics. Do not count compiled-but-unused ordinary layouts as completion. Remaining six-item audit also includes known continuous blend/orientation and pelvis actor-motion approximations; keep the original goal active.


### Native variable vector layout: first runtime consumer

- Previous comparison turn revalidated 68 source/resolved schema hashes against the actual reference Content files, all matching, and confirmed the ordinary runtime still used the fixed layout. This turn builds and connects the pending native vector implementation; the six-item objective remains active.
- Added typed Position/Velocity/Heading channels with ordered axes, explicit query policy, sample time, weights and normalization groups. Database owns its copied immutable layout; matcher owns its copied normalized query. Invalid setters preserve prior state, mixed physical/normalized velocity pools reject, and basic query APIs reject variable-layout databases. Pose offsets remain zero-only; curves are not yet supported. Owner-thread/lifetime/no-callback and captured desired-yaw contracts are documented. No persistent-format/module/lifecycle change or deliberate architecture exception.
- Root sampling supports arbitrary past/future trajectory times, loop rigid-cycle accumulation and nonloop extrapolation. Sample grid uses imported rate, includes nonloop grid endpoints, and preserves the old incremental grid for legacy layouts. Normalization and query encoding share vector helpers; actual per-schema normalization-set scope is still missing.
- First compile exposed private AnimClip sampling access from a free helper; made sampler a private nested database implementation rather than widening public access. Explicitly touched 36 native consumer TUs for changed class layouts. Configure/build eve and unit_test passed (build/native-schema-build2.log).
- Example reference_selection now configures its bank from imported PSS_Default through setFeatureLayout and submits timestamped setFeatureQuery values. This replaces its fixed native encoder, but ALL ordinary chooser databases still share Default. Per-schema routing, Phase and schema-specific query prediction remain required; do not count this as their completion.
- build/native-schema-tests.log PASS20 existing/new motion tests. Added a third boundary test and rebuilt unit_test; build/native-schema-boundaries.log PASS3 schema tests, covering copied layout/query, invalid replacement, velocity discrimination, 20 Hz grids, arbitrary -.2/+1.35 s trajectory selection across loop/nonloop boundaries, missing/duplicate query times and mixed-unit normalization rejection. Full-quaternion query invariance and rotating-cycle cases still need dedicated coverage.
- Fresh build/native-schema-runtime/result.json PASS55 real-engine checks, including twelve hand contacts,24 repeated traversals, moving platforms, high-platform movement/falling and collision/render authority. Actual startup reports reference bank1879 clips,106 databases,bake1.235s. Engine-owned walk and low-mantle contact frames inspected; distant frames are not new close-up or complete coordination evidence. Runtime verifier terminated normally and stopped its engine.
- Binding strict check, module dependency check, test manifest and diff check passed. Architecture source gate passed with PYTHONUTF8=1 (build/native-schema-architecture.log); initial non-UTF8 run failed while decoding git diff, not on a contract finding. All9 architecture fixtures passed in153.361s (build/native-schema-fixtures.log); handle31847 terminal.


### Schema-driven query times and evaluated Phase source data

- Previous goal turn was progress: native variable vectors compiled, Default consumer connected, PASS55 runtime. This turn removes the remaining five-time query list and four-frame movement-history assumption. Bank owns sorted distinct trajectory times read from its imported layout; actual capsule positions, velocities and yaw now have injected simulation timestamps and retained history bounded by imported schema offsets. Query interpolates position/velocity and shortest-arc yaw, extrapolates only before the initial history sample, and returns copied values. Positive horizons reuse acceleration/ballistic prediction at the actual requested time. Reset/bank switch clears movement history and its clock. No new API/schema/module/lifecycle change in runtime scripts.
- Expanded real-engine probe asserts the bank uses native variable channels, verifies actual Jump -.2/0/.2/.5 and Relaxed_Loops final1.35 horizons, history interpolation, cold extrapolation, yaw wrap and no mutable aliasing. Fresh build/schema-query-times-runtime/result.json PASS55 full runtime checks; engine-owned sustained-Relaxed-walk frame inspected. Verifier84779 exited0 and stopped its owned engine. Per-schema bank routing is still pending, so current actual bank uses Default times; other imported layouts are covered by the extraction probe, not claimed as active matching.
- Direct UE5.8 Runtime/Private/PoseSearchFeatureChannel_Curve.cpp audit: source Curve uses one scalar, copies continuing values when eligible, otherwise samples pose-history curves at the channel offset. Phase must not be recast as a custom circular angle. Imported animation metadata contains97 Phase-bearing sequences.
- Refactored existing UE contact exporter evaluation into an internal fixed-channel helper, preserving old contact format/CLI. New ue_export_pose_curves.py uses the same UE evaluator for Phase at240Hz plus all authored key times/endpoints. New intermediate eve.unreal-pose-curve-samples/1 records all sequences including absence, unknown-field reject and source hashes; checks source hash before and after evaluation, atomically publishes, and never saves source packages. Existing contact tests retain nonfinite/atomic failure coverage; added actual wrapper test for nonlinear evaluator values and mid-export source mutation preserving old output. Five Python tests pass (build/pose-curve-export-tests.log).
- Actual reference UE commandlet49018 exited0 (build/pose-curves-ue-export.log), producing build/ue-reference/pose-curve-samples.json:2008 entries,97 Phase curves,4220406 JSON bytes. This is unreduced intermediate source data, not runtime payload. Phase reduction/EVFC compilation, native scalar channel indexing/query and evaluated curve-history consumption remain required. Do not mark Phase or six-item objective complete.
- Source architecture gate passed before exporter addition (build/schema-query-times-architecture.log); final source gate after exporter changes also PASS (build/schema-query-times-architecture-final.log), handle32004 terminal. All9 fixtures passed in182.881s (build/schema-query-times-fixtures.log), handle63752 terminal. No deliberate architecture exception; no native layout/API change this turn. Diff check passed.


### Phase runtime binary compilation

- Previous goal turn was progress: schema-driven prediction/history and UE Phase extraction. Current turn adds scripts/build_unreal_pose_curves.py, using a shared explicit-schema/channel reduction helper extracted from the existing contact compiler. Contact input/output versions and accepted channel names stay unchanged. Pose compiler rejects wrong source schema, unexpected channels/fields, stale hashes and incomplete source coverage, then emits the existing generic EVFC/1 format. Publication failure preserves the previous output and removes its temporary file. No new native API, cross-domain lifecycle or module dependency; no deliberate architecture exception.
- Actual source artifact compiled to examples/climbing-motion-matching/assets/ue-local/selection-v1/pose-curves.evc:197802 bytes for2008 source records,97 present Phase curves. build/pose-curves-error-report.json verifies111545 playable UE sample values against decoded Float32 output:1068 retained keys,max error0.0008114851. The .001 reduction bound applies at the evaluated samples, not analytically between240Hz samples. Source post-roll is validated but playback ends at the actual sequence duration.
- Eight Python tests PASS (build/phase-compiler-tests.log): nonlinear source evaluation, signed Phase/error/absence/input immutability, incompatible schemas/channels, stale/incomplete source rejection, Float32 collision rejection and failed atomic publication. Existing contact nonfinite/absence/publication coverage retained.
- Runtime FOOT_PLACEMENT_PROBE now loads this artifact with the native AnimCurveLibrary and compares a real Phase sample at.5s to UE value0.5264031887. Fresh build/phase-library-runtime/result.json PASS55 complete scenario checks; engine-owned moving-platform contact frame inspected (distant view). Verifier74943 exited0 and stopped its owned engine. This proves native curve loading/evaluation only; Phase still does not participate in motion feature scoring.
- Direct source audit confirms Curve IndexAsset uses sampleTime+channelOffset and scalar curve value. Query uses continuing feature values when eligible; otherwise requests evaluated pose-history curves, defaulting to0 when absent. Actual compiled schemas with Phase are PSS_Default_Mover and PSS_Relaxed_Loops, both current and-.0333s with USE_CONTINUING_POSE. Next required work is native scalar channel indexing/query plus curve history and per-schema bank routing; all six objectives remain active.
- Architecture source gate PASS (build/phase-compiler-architecture.log), handle71420 terminal. All38 related Python tests PASS (build/phase-compiler-python-regression.log). Fixture handle85778 subsequently exited0: all9 cases PASS in231.435s (build/phase-compiler-fixtures.log). Diff check passed.


### Native scalar curve feature indexing and queries

- Previous goal turn was progress: reduced Phase EVFC compilation and native evaluator probe. Current turn adds MotionFeatureKind::Curve, named scalar channels with arbitrary bounded time offsets, and copied MotionFeatureCurveSample queries. Curve axes must be scalar1/sourcePose; vector channels reject nonempty curve names. Normalization treats curves as dimensionless signed scalars; continuing query policy uses the existing eligible-playhead feature substitution. Missing authored channels in known sources evaluate to0; missing query channel/time rejects rather than guessing.
- MotionDatabase::setFeatureCurves(bytes,sources) atomically owns a decoded EVFC snapshot plus copied source names keyed by already-added clip identity. Rejects invalid/incomplete/missing/conflicting sources and post-bake changes. Bake validates required curve provider/source presence before frame mutation. The existing AnimCurveLibrary const implementation has no mutable sampling state; contract now explicitly permits concurrent const sampling while the owner prevents load/destruction, supporting database bake workers. Public API ownership/thread/no-callback/failure contracts documented; no new persistent format, module boundary, ECS state or deliberate architecture exception.
- Script loadFeatureCurves bridges filesystem bytes to the checked native API. setFeatureLayout accepts curve names as field13; setFeatureQuery now accepts a fifth curve-array argument [name,seconds,value], and the example's sole ordinary caller supplies[] until per-schema routing/curve history is connected. Imported channel mapper now converts scalar channels directly. No false claim that ordinary Default matching uses Phase.
- Explicitly touched all affected native consumers for header layout/signature changes. Native eve/unit_test build PASS (build/native-phase-build.log). Full motion tests PASS22 (build/native-phase-tests.log); after adding continuing-policy test, rebuilt unit_test and all5 schema tests PASS (build/native-phase-policy-tests.log). Scalar tests prove positive/negative/absent Phase chooses different clips with identical bone poses, invalid replacement preserves the query/data, copied binary lifetime, negative-time nonloop sampling and actual Character vs Continuing policy behavior.
- First runtime build/native-phase-runtime completed PASS55, but a mid-run mapper edit failed hot reload due existing characterSkins.part serialization limitation. Started fresh verifier92000 at build/native-phase-imported-runtime; its probe uses the actual imported Relaxed_Loops Phase definition, EVFC source data, native curve index and explicit query, expecting matching cost0 at.5s. Do not use the first run as proof of the latest mapper. Fresh verifier92000 exited0 with full55-check PASS (build/native-phase-imported-runtime/result.json), including imported scalar index/query cost agreement. Engine-owned high-platform-falling frame inspected; verifier stopped its owned engine.
- Strict bindings, module dependency, test manifest and diff checks PASS. Source architecture gate before final mapper/test edits PASS (build/native-phase-architecture.log), handle14071 terminal. Final source gate11248 PASS (build/native-phase-architecture-final.log); fixture6694 PASS9 in211.154s (build/native-phase-fixtures.log); both handles terminal. Remaining: evaluated curve-history blend/offset queries, actual per-schema banks and routing, broader root quaternion/normalization-set/notify fidelity, plus the other six-item audit gaps. Objective remains active.


### Evaluated Phase and pose-curve history

- Previous goal turn was progress: native scalar indexing/query, matching discrimination and imported-channel proof. This turn extends the existing contact-curve evaluator to a third signed Phase channel, sharing current/outgoing clip selection, inertial value/derivative transition, Standard entry blend and advancing montage exit-tail evaluation. It does not invent a second independent Phase blend state. FootPlacement continues reading indices0/1; Phase owns index2. Both EVFC libraries and full source coverage validate before publication; reset restores all three channels.
- Selection state owns copied {time,value} Phase history recorded after actual curve evaluation on the same injected pose clock as rendered-root history. Imported layout retention bounds storage; zero-dt reevaluation is idempotent. referencePoseCurveQuery extracts distinct Phase/time requests from the layout and samples evaluated history, with cold0, linear interpolation and endpoint clamping. Actual UE PoseSearchHistory.cpp LerpEntries/GetCurveValueAtTime source confirms default non-extrapolated linear clamped sampling. Unknown named curves reject explicitly. No new native API, persistent format, module or cross-domain lifecycle, and no deliberate architecture exception.
- Ordinary setFeatureQuery now requests layout-driven curve queries instead of a hardcoded empty array. Current bank.queryLayout is still Default, so actual ordinary searches still have no curve channels; Relaxed schema routing is not yet complete. History/evaluation executes on real animation frames and is tested with explicit imported Relaxed_Loops curve requests.
- CONTACT_CURVE_PROBE fixture now includes signed Phase +1/-1. Expanded checks verify source-active evaluation, retained +1 at a switch to raw -1, history query using blended value, zero-dt history idempotence, eventual negative-value convergence and a .6s outgoing Phase exit tail into an absent-curve clip. Existing foot/contact tests retained and test-injected libraries are restored.
- Fresh build/phase-history-runtime/result.json full55-check PASS, including all new Phase checks plus repeated traversal, moving-platform contact and high-platform falling. Verifier82356 exited0 and stopped its engine; no script edits were made during this run. Engine-owned conditional-mantle-exit frame inspected. Architecture source gate60236 PASS (build/phase-history-architecture.log); fixture15934 PASS9 in171.262s (build/phase-history-fixtures.log), handle terminal. Diff check PASS. Keep full six-item goal active; next major step is actual schema-specific banks/routing, then normalization-set/notify fidelity and other remaining coordination gaps.

- Next routing sizing audit from current selection.json:106 DBs reference16 schemas,1069 distinct source clips,1192 total distinct clips across schema groups (Default608, Default_Mover22, Idle4, Jump60, Relaxed_Idle4, Relaxed_Jump59, Relaxed_Loops35, Relaxed_Pivots132, Relaxed_RunSpins30, Relaxed_Slide8, Relaxed_SlideExit12, Relaxed_StandTurn8, Relaxed_Starts74, Relaxed_Stops54, Relaxed_WalkSpins30, Stop52). Build actual schema membership rather than replicating1879 clips across every layout; maintain explicit local-to-source/global identity for selection/traversal consumers. No such routing change has been made yet.


### Ordinary per-schema routing connected

- Previous turn was progress: evaluated Phase history with full55 regression. Current reference_schema_banks.nut builds all16 imported ordinary layouts from actual member sources (1192 distinct clip memberships across1069 unique sources), while retaining the master1879-source catalogue for traversal/contact data. Index-local source maps prevent matching IDs from leaking into master lookups. Candidate schemas derive from current Chooser results; scoring uses each native layout and its timestamps/Phase history, with a fresh matcher for inactive schemas so stale playheads cannot masquerade as continuing poses. Equal scores prefer the current eligible schema.
- Candidate-range updates occur on selection changes or fresh inactive matchers, rather than rebuilding native frame masks every frame. Query trajectories cover the union of selected schema times. Query poses are copied before adding component position/rotation and actual resolved velocity so world-space pose velocities include character travel without altering the rendered pose. Schema switches queue a continuous transition and update the active matcher/database/clip array centrally. Ordinary matching no longer invokes appendReferenceAirRanges; old helper is now unused and remains to remove during cleanup.
- First actual Jump build rejected a mixed physical/normalized velocity normalization group. This exposed an INCORRECT earlier defensive rejection. Direct UE PoseSearchFeatureChannel::CanBeNormalizedWith checks class/cardinality/explicit group, not bNormalize; the real Jump schema uses such a pool. Removed rejection and added explicit MotionFeatureLayout.normalizationLengthScale (default1 for metres,100 for imported centimetres). Physical vector channels scale before pooling/query normalization; headings, normalized velocities and scalar curves do not. Degenerate physical thresholds scale consistently. This preserves generic API units and actual source grouping instead of pretending mixed units cannot occur. Earlier log statements/tests claiming such groups should reject are superseded.
- Updated script setFeatureLayout signature(rate,channels,lengthScale), both callers, docs and bindings. C++ aggregate default remains metre-based. Native build PASS; all6 schema tests PASS including new mixed-pool source-scale/index-query agreement (build/per-schema-native-tests.log). Public scale ownership/value/validation contracts apply; no new persistent format/module/cross-domain lifecycle or deliberate architecture exception.
- build/per-schema-runtime failed init on the now-corrected mixed-pool rejection. build/per-schema-runtime2 built all16 and reached high-platform checks, then the verifier itself used an obsolete global clip index. Fixed assertion mapping through referenceMatchedSource and strengthened selected-schema/dimension assertions. Subsequent build/per-schema-runtime-final passed high-platform movement/falling but caught a REAL montage-exit regression: schema-switch pose blending ran after per-bone exit blending and modified feet that should already be released.
- Added explicit transitionBeforeExit for schema switches, reset on generic queue/cancel. Main pose pipeline now performs schema-switch blending before the montage exit layer, retaining per-bone exit authority. No assertion threshold was relaxed; failure diagnostics now include bone/dot. Full fresh build/per-schema-exit-order/result.json PASS55, including all original contact/traversal/high-platform/exit checks and actual selected-schema assertions. Verifier32052 exited0 and stopped its owned engine.
- Measured successful schema startup: all16 built (Default30,Idle34,Jump39,Relaxed_Loops25 etc), overall reference selection load8.194s,bake1.449s in build/per-schema-runtime2/runtime.log. This is build timing, not a measured frame-rate claim. Phase is now part of ordinary searches when those schemas are selected, no longer only an isolated probe. Cross-schema NormalizationSet/common-schema scope, notify exclusions/reselection-history and full orientation/blend/pelvis parity remain unfinished; keep all six goals active.
- Architecture source gate PASS before exit-order fix and again after it (build/per-schema-exit-architecture.log), final handle99146 terminal. All9 architecture fixtures PASS220.151s (build/per-schema-fixtures.log), handle79424 terminal. Strict bindings/module dependencies/test manifest/diff checks PASS. Pending cleanup includes unused old air filter, preview-to-reference query-facing reset, additional direct world-space/angular query tests and measured multi-schema frame cost.

- Engine-owned sustained-Relaxed-walk and high-platform-falling frames from per-schema-exit-order inspected after the complete pass. These are representative frames, not proof of full-body coordination at all turn/obstacle angles.

### Source BlockTransition entry protection

- Direct UE 5.8 source comparison showed that `PoseSearchAssetIndexer` stores `BlockTransition` independently from database exclusion: protected poses remain valid continuation, while search filters reject them as new destinations. `FAnimationAssetSampler` evaluates notifies through a centered `1/120 s` interval. The previous runtime only implemented `PoseSearchExcludeFromDatabase`, so motion matching could jump into protected middles of stops, turns, landings and other authored sequences.
- Added a source-hash-verified `eve.unreal-transition-notifies/1` export and selection schema 7. The exporter read the user-supplied reference project without saving assets: 2008 AnimSequences, 663 sequences containing 671 BlockTransition windows. UE Python 5.8 exposes effective trigger time and duration but not `EndTriggerTimeOffset`; the artifact declares `EffectiveTriggerPlusDuration`, and the compiler expands both ends by half the source extraction interval. Invalid, stale, duplicate, incomplete or unknown-field input rejects before publication.
- `MotionSearchRange` now owns copied transition-block intervals. `setCandidateRanges` validates and atomically builds a separate per-frame block mask, unioning blocks across duplicate ranges. Search applies it only to new candidates; eligible continuation crosses a block normally, while an ineligible current clip still exits. Inclusive-start/exclusive-end boundaries are deliberate, and an expanded end beyond clip duration protects the terminal sample. Script ranges now require six values and selection-v7 supplies the actual blocks.
- Native build PASS. All 27 `animation.motion*` tests PASS, including fresh-entry rejection, protected continuation, ordinary membership authority, duplicate-range union, exact boundaries, copied inputs, all-blocked recovery and atomic invalid-input failure. All 34 selection/layout/schema Python tests PASS. Regenerated selection contains 106 databases, 1246 sample ranges and 610 ranges carrying real block windows.
- Fresh `build/transition-block-runtime/result.json` PASS all 55 engine scenarios, including starts/stops/turns/jumps/lands, repeated traversal, montage exits, moving/high platforms, collision authority and falling. Engine-owned traversal contact, high-platform falling and conditional exit frames were inspected. Strict bindings, module dependency, test manifest and diff checks PASS. This completes the first newly identified parity gap, not the overall six-item objective; per-pose cost notifies, continuing cost override/history, shared NormalizationSet and full orientation/contact coordination remain active.
- Public API/format rules apply: owned copies, bounded validation, checked `Result`, owner-thread/no-callback contract, schema bump with reject policy and explicit regeneration. No new module boundary, ECS lifecycle or deliberate architecture exception.

### Source pose and continuing cost fidelity

- UE source confirms two independent costs. Every indexed pose starts with its database `BaseCostBias`, a `PoseSearchModifyCost` state replaces that value, and looping bias is then added. Continuing evaluation separately starts from the result database's `ContinuingPoseCostBias`; `PoseSearchOverrideContinuingPoseCostBias` replaces it at the current sampled pose. Both remain additive to feature dissimilarity. The previous runtime carried only base/looping candidate bias and a global 10% switch guard, so it could neither rank authored local segments nor retain stops, turns, jumps and lands with their database-specific policy.
- Extended the read-only source snapshot to `eve.unreal-pose-search-notifies/2`, preserving BlockTransition plus finite local candidate and continuing cost values in original event order. Current reference evidence is 2008 sequences, 671 block windows, 22 candidate-cost overrides and 63 continuing-cost overrides; authored values span -10 through -0.01. Selection schema 8 now retains database continuing bias (14 distinct values among 106 databases) and the two source override lists. Source hashes, complete coverage, time basis, fields, cardinality, ordering and finite values validate before publication.
- `MotionSearchRange` now copies separate candidate and continuing defaults/overrides. During atomic frame selection, the last active override replaces its default, matching UE notify iteration. If one clip/frame is supplied by multiple selected databases, the continuing bias stays paired with the range having the lowest candidate cost rather than independently taking an impossible combination. Search adds continuing cost only to the eligible live playhead; new candidates receive candidate cost only. Looping bias is folded only into candidate defaults/overrides in the example.
- Native rebuild PASS. All 30 `animation.motion*` tests PASS, including local override order/end boundary, database continuing retention, local continuing override switching, duplicate-range cost pairing, invalid-input atomicity and all prior BlockTransition/schema/curve contracts. The 21 focused compiler tests PASS. Regenerated production selection contains 1246 candidate ranges; 21 selected ranges carry candidate overrides and 88 carry continuing overrides because source clips can belong to multiple databases.
- Fresh `build/pose-cost-runtime/result.json` PASS all 55 production scenarios. Stop, pivot and landing engine-owned frames were inspected after the pass; no missing render/collision regression appeared. This completes the second newly identified scoring gap, not the overall six-item objective. Shared NormalizationSet, pose-reselection history/full search threshold fidelity, orientation warping and unified contact coordination remain active.
- Public API/persistent format rules apply: owned bounded intervals, checked atomic `Result`, owner-thread/no-callback lifetime contract, reject-policy schema migration and explicit regeneration. No module boundary, ECS lifecycle or deliberate architecture exception.

## 2026-09-12 UE NormalizationSet parity

- Exported four PoseSearch normalization sets from the reference project without saving source assets.
- Selection schema 9 preserves the three sets referenced by the 106 selected databases and 23 additional databases used only as normalization input.
- Runtime banks are keyed by `(schema, normalizationSet)`. `PSS_Stop` now has distinct Dense and Extreme Sparse statistics.
- Native variable-layout databases accept ordered inclusive normalization ranges; duplicate entries preserve UE dataset multiplicity.
- Evidence: 77 focused Python tests, 32 native motion tests, and all 55 real-engine reference scenarios pass. Engine screenshots were inspected for traversal contact and high-platform fall/landing.
- Measured runtime: `load=17.137s`, `bake=10.404s`; offline compiled feature/statistics caching remains necessary.
- Remaining visible gap: the high-platform descent can select a descending interval from `M_Neutral_Jump_F_Start_Walk_*`, leaving the torso too upright despite vertical-phase filtering.

### UE pose-reselection and strict-cost switching

- Direct UE 5.8 source confirms the ordinary MotionMatching node defaults: PoseJumpThresholdTime=(0,0), PoseReselectHistory=.3s and SearchThrottleTime=0. The search compares candidates directly with continuing cost; the prior EVEngine 10 percent improvement gate had no source equivalent.
- Reference schema matchers now use zero-frame jump exclusion and .3s reselect history. Native matchers retain an owned, bounded frame-age history, update it only from positive simulation steps, keep continuation eligible and reject invalid configuration atomically. Recent lookup is limited to current candidate frames.
- Removed the artificial 10 percent switch margin. Tests prove a strict .05 cost improvement switches, recent poses cannot be jumped back to, continuation remains valid and expiry restores eligibility.
- Native build and all34 motion tests PASS. Fresh build/pose-history-runtime/result.json PASS55; high-platform descent remains visually too upright and still selects M_Neutral_Jump_F_Start_Walk_Rfoot_0 at .15s, so this search-policy correction does not claim that separate issue fixed.
- Public Result/ownership/thread requirements apply; no persistence, module, ECS or cross-domain lifecycle change and no deliberate exception.
# 2026-09-12 update: orientation warping parity

- Added the locomotion BlendStack OrientationWarping stage after pose blending and before OffsetRootBone/contact solvers.
- Matched the authored Sandbox graph values: graph-driven locomotion direction, five spine bones, `ik_foot_root`, both IK feet, `DistributedBoneOrientationAlpha=0.5`, `LocomotionAngleDeltaThreshold=135`, and `RotationInterpSpeed=8`.
- Root motion direction sampling now joins the end and beginning displacement of looping clips instead of treating the loop boundary as a large backwards step.
- `build/orientation-warp-runtime-final/result.json` passed the 55-scenario real-engine suite, including explicit threshold/interpolation/bone-chain assertions. Representative strafe and high-platform images were inspected; orientation warping improves locomotion turning, while the still-upright falling pose remains an air-schema/state issue.

# 2026-09-12 update: airborne phase and dynamic range parity

- Connected the existing airborne range builder to every selected Jump schema. It was previously dead code, so root-trajectory phase filtering never constrained the native matcher.
- Descending ranges now require both authored foot contact curves to be released. This rejects stationary, planted jump anticipation without animation-name rules.
- Rebuild airborne ranges every simulation step because vertical velocity and time-to-land change continuously. The old path only replaced ranges when the Chooser database list changed.
- Fixed native continuation eligibility: a live playhead must be represented by a nearby frame in the replacement ranges. A distant valid interval in the same clip can no longer retain an invalid phase.
- Added `animation.motionMatching.replacementRangesInvalidateDistantSameClipContinuation`; all 35 focused motion tests pass.
- `build/air-phase-final/result.json` passes all 55 real-engine scenarios. The high-platform capture now selects authored walk-off animation `M_Neutral_Jump_F_Off_Walk_Rfoot_0 @ 1.15`, inside its active `[1.067,1.300]` range, instead of planted `M_Neutral_Jump_F_Start_Walk_Rfoot_0 @ 0.15`.

# 2026-09-13 final reference-parity closure

- The final hand-contact pass now uses the UE-authored FrontLedge window as authority. It solves the wrist from a world-space middle_03 fingertip target on the actual obstacle top; Mantle retains its authored left-hand support while Vault/Hurdle constrain both hands. The contact response is 8 ms, source poses remain immutable, and constraints release with the traversal window.
- Foot Placement is followed by the takeoff-ground constraint before hand IK, so pelvis adaptation cannot reintroduce foot penetration while satisfying the ledge contact.
- The verifier now distinguishes reference InAir/landing state from the KayKit irState, accepts the intentional native cancelled terminal produced by conditional montage exit, compares modifier movement against a walk baseline, and verifies supporting fingertips rather than requiring the wrist origin to cross the ledge.
- A persistent baked-index cache prototype was measured and removed: 17/17 cache hits still spent 11-14 seconds rebuilding in-memory frame structures and worsened total startup. The shipped path keeps deterministic eight-worker parallel bake and creates no large save files.
- Final evidence: uild/reference-parity-final/result.json passes the complete 55-scenario real-engine suite. Representative engine-owned low/high hand-support and landing frames were inspected. nimation.motionMatching|motionSchema native tests pass 35/35; UE Python tests pass 77/77; strict bindings, binding-gap metadata, module manifest, dependency graph, test manifest and diff checks pass; architecture source gate and all 9 fixtures pass.
- Applicable architecture rules: public Result/ownership/thread contracts, copied matcher inputs, persistent selection schema versioning/reject policy, single capsule authority, and module boundaries. No deliberate exception.

# 2026-09-13 runtime sequence parity follow-up

- Runtime hitch audit found two independent synchronous costs: candidate-range publication scanned every frame in the schema bank, and script-level schema search was followed by MotionMatcher's own timed search. MotionDatabase now records contiguous per-clip frame offsets, and the reference layer owns search scheduling. All 36 animation.motionMatching/motionSchema tests and the climbing candidate performance test pass.
- Airborne sequencing now uses the imported CMC families without a transition-frame database scan: Stand/Walk/Run/Sprint Start, Walk/Run Off, Fall loop, and Stand/Walk/Run Light/Heavy Land. Lfoot/Rfoot is selected from the live pose at takeoff and again immediately before ground contact. Ground OrientationWarping is cleared and disabled during the airborne sequence.
- `scripts/verify_reference_air_sequence.py` drives the real engine and passes all 12 sequence families. The full climbing verifier passes traversal, 24-cycle reentry, gravity, contact landing and deck collision with the new sequence path.
- Ground sequence tracing now runs outside the obstacle lanes and records every selected source for Walk/Run/Sprint start-loop-stop, forward-to-back pivot, strafe and slide. It exposed repeated Lfoot/Rfoot slide-exit reselection; unchanged Sliding context now searches once on entry and plays the selected authored range through. Start/Stop/Pivot/Transition one-shots likewise retain their selected imported range while their movement condition remains valid, but opposite input or movement/stop changes may interrupt. `build/reference-sequence-trace-4/runtime.log` confirms the slide sequence is now `KneesOut_trans_FootOut -> FootOut_Out_Moving_Run -> Sprint_to_Run` with no alternating-foot churn, and the 12 airborne families remain passing.
## 2026-09-13 locomotion transition parity

- Reconstructed `SandboxCharacter_CMC_ABP.Get_MMBlendTime` from the exported
  reference graph: ordinary locomotion uses 0.50 s, upward jump entry uses
  0.15 s, landing from InAir uses 0.20 s, and other airborne changes use
  0.50 s. The example now applies those values instead of a global 0.14 s.
- Reconstructed `Get_MMInterruptMode`: core MovementMode/MovementState changes
  interrupt the selected database; a gait-only change interrupts while Moving,
  but idle Stop playback remains `DoNotInterrupt`. Non-looping Stop clips now
  hold until movement resumes rather than chaining Sprint/Run/Walk stops.
- Pivot classification now compares current velocity direction with requested
  input acceleration, matching the reference predicate. Collision state is
  calculated from the current capsule move, with a minimum effective-speed
  threshold so a reversal's zero crossing is not mistaken for a wall.
- `build/reference-sequence-pivot-final/runtime.log` records the verified
  reversal `Run_Loop_F -> Run_Pivot_F_B_Rfoot -> Run_Start_B_Lfoot ->
  Run_Loop_B`. `verify_reference_air_sequence.py` now asserts that a reverse
  trace visits a Pivot clip and that each Stop trace selects only one clip.
  The same run passed all Stand/Walk/Run/Sprint starts, Off_Walk/Off_Run, and
  Stand/Walk/Run light/heavy landing families.

## 2026-09-13 pose-history feedback and terminal-stop parity

- Split pose history by its actual graph owner. Pose Search now reads the raw
  Motion Matching/BlendStack output, before OrientationWarping, root offsets
  and limb IK; traversal feature extraction retains the preceding fully
  rendered pose so authored support motion remains measurable. This prevents
  procedural corrections from feeding back into the next search and causing
  progressively incompatible, twisted pose selection.
- The native matcher normally searches when a non-looping clip reaches its
  end. Reference Stop playback now suppresses that terminal search while idle,
  implementing the graph's `DoNotInterrupt` result. Fresh trace initially
  exposed `Walk_Stop_F_Lfoot -> Walk_Stop_F_Rfoot` at frame 86; after the fix,
  Walk, Run and Sprint each retain one Stop source until movement resumes.
- The imported corpus count is 2,008: 1,879 UEFN Mannequin character clips and
  129 Echo/Manny/Quinn/MetaHuman rig-pose or correction assets. Reference
  selection loads exactly the 1,879 character clips. The HUD now distinguishes
  the reference character pool and active schema bank from the total imported
  preview corpus instead of implying that all skeleton-support assets are
  ordinary match candidates.
- Fresh evidence: `build/reference-stop-terminal-fix/result.json` passes all 12
  air families and its ground trace records the authored start/loop/single-stop,
  pivot, strafe and slide sequences. `build/reference-stop-terminal-regression/result.json`
  passes the complete 55-check real-engine suite, including 24 consecutive
  traversals, contacts, moving/high platforms and montage exits. The resulting
  interactive current-worktree engine remains running for manual review.

## 2026-09-13 Foot Placement alpha and pose continuity

- A new per-bone continuity trace separated the raw matcher/BlendStack pose
  from the final postprocessed pose across turns, jump/landing and traversal.
  It found a deterministic final-pose-only right-leg failure during ordinary
  forward running: thigh 78.68 degrees, calf 151.46 degrees and foot 63.99
  degrees in one frame while the right Foot Placement alpha was exactly zero.
- The Foot Placement implementation calculated the authored interpolation
  alpha but called the two-bone solve with a hard-coded weight of one. It now
  passes the actual foot alpha, so an unaligned swing leg retains its authored
  pose and partial alignment blends the entire leg solve.
- Repeating the identical 810-frame diagnostic produced zero local bone
  rotation changes above 60 degrees. The earlier eight events included the
  final-only running knee flip and a raw landing transition; both are now
  below the diagnostic threshold after the existing transition smoothing.

## 2026-09-13 Default Mover and Relaxed sequence parity

- Direct UE 5.8 inspection of `DefaultLevel`, `GM_Sandbox` and the spawned pawn
  confirms the project runs `SandboxCharacter_Mover` with
  `SandboxCharacter_Mover_ABP`. That graph binds
  `CHT_PoseSearchDatabases_Relaxed` and the Mover traversal table; the previous
  example defaulted to the CMC Dense/Neutral path.
- The focused read-only exporter now imports all 34 Mover traversal montages.
  Generated selection and warp-distance data use the Relaxed montage sources.
  The global export still rejects the reference project's duplicate
  `PSD_Dense_Stand_Walk_Stops` short name instead of silently choosing one.
- Runtime defaults now match the project settings: Relaxed selection and
  strafe style enabled. Mover's one-frame empty bank handoff holds the outgoing
  pose, contact curves and orientation source while publishing the authored
  BlendStack duration; pose, curve and root-offset consumers no longer index
  an invalid matcher result.
- `build/reference-mover-regression-final-7/result.json` passes all 55 real
  engine scenarios. The final default-mode continuity trace reports zero local
  bone rotation jumps above 60 degrees, and 28 focused import/compiler tests
  pass. The exact air-family verifier also passes Stand, Walk, Run, Sprint,
  Off_Walk, Off_Run and all light/heavy landing families. The verified
  current-worktree engine remains open for manual review.

## 2026-09-13 direct Mover input and reversal parity

- A real PIE capture now injects the reference project's Enhanced Input keys
  and records 330 frames of component-space bones and capsule velocity. It
  proves bare W is Run at 3.75 m/s, Ctrl+W is Walk at 1.65 m/s, and Shift+W is
  Sprint at 5.85 m/s. The example controls and HUD now use those exact roles.
- Ground velocity now retains a jerk-limited acceleration state. Against the
  same idle/forward/reverse/stop trace, velocity RMSE is 0.12 m/s after
  accounting for the reference PIE input-event latency. The reverse branch is
  constrained to authored strafe Pivot samples, preventing a 180-degree Turn
  from winning by raw cost and removing the observed 24.7 cm one-frame foot
  displacement.
- Foot Placement now blends its whole-leg solve weight across plant-state
  changes. Unplanted legs are no longer snapped into a fully weighted IK solve,
  which previously produced deterministic 90-110 degree calf flips after
  traversal and during direction changes.
- Fresh evidence: `build/reference-mover-regression-sequence-final/result.json`
  passes all 55 real-engine scenarios; `build/reference-sequence-pose-continuity-6/runtime.log`
  reports zero final or raw local-bone changes above 60 degrees; and
  `build/reference-air-sequence-final` passes all 12 takeoff/fall/landing
  families. The latest current-worktree engine remains running for review.

## 2026-09-13 measured DefaultLevel hurdle parity

- A PIE environment inventory identified the reproducible Y=-900 hurdle lane:
  a 20 cm approach deck, a 50 cm-deep obstacle rising to 120 cm, and a pawn
  running on the deck's side boundary. The EV course now preserves those
  relative dimensions and the ledge interaction centre's 30 cm inward clamp.
- With W held and traversal requested at frame 142, both runtimes select
  `M_Relaxed_Traversal_Hurdle_1_0_stand_F_V2_Rfoot` at entry time zero and
  leave it after frame 202. Capsule-trajectory RMSE fell from 0.353 m to
  0.070 m; at frame 172 the reference/EV vertical displacements are
  1.002/1.003 m, and final forward displacements are 1.713/1.702 m.
- The imported hurdle montage already contains the reference limb pose. The
  generic low-ledge foot clamp and approximate hand-contact solve displaced
  the final hands by as much as 0.51 m. Hurdles now retain the authored pose;
  left/right hand RMSE against PIE fell to 0.088/0.032 m and pelvis RMSE to
  0.066 m. Mantles and other supported traversal actions retain their contact
  corrections.
- `scripts/compare_reference_runtime_sequences.py` records this evidence from
  paired UE/EV captures. The air-family verifier remains green and the final
  pose-continuity trace reports no local-bone jump above 60 degrees. The full
  selection verifier is being updated for the reconstructed course extents.
