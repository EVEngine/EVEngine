# Default owner: the engine maintainer.
#
# As the team grows, split ownership per area so pull requests are routed to
# the people who know the code (this also keeps agents working on disjoint
# directories from stepping on each other's review queue). Example once a
# team exists:
#
#   /src/modules/graphics/ @EVEngine/graphics
#   /src/modules/scene/    @EVEngine/scene
#   /src/modules/*/vulkan/ @EVEngine/vulkan
#   /test/                 @EVEngine/testing
#
# Until then every path is owned by the maintainer.
* @sunxfancy
