# Vision pre-filter service deps (thin HTTP gateway).
# The heavy lifting is done by llama.cpp's `llama-server` (separate binary),
# so NO torch / transformers / bitsandbytes are needed here.
# Python 3.10+ recommended.

fastapi>=0.141.1
uvicorn[standard]>=0.29
pydantic>=2.6
requests>=2.31
